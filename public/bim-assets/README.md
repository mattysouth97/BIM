# Slot-default BIM assets

These files satisfy `src/lib/bim/asset-slots.ts`. Call
`publishAuthoringAssets()` (already wired in `providers.tsx`) so
`resolveAssetSlot` returns `source: "manifest"`.

The full 46-family type library is `/models/authoring/`.
