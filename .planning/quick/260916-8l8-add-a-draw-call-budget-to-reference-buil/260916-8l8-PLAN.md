---
phase: quick-260916-8l8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/reference-buildings/service-layer-budget.ts
  - src/lib/reference-buildings/__tests__/service-layer-budget.test.ts
  - src/lib/reference-buildings/__tests__/service-layer-budget-failure.test.ts
  - src/lib/reference-buildings/__tests__/service-layer-budget.glb.test.ts
  - docs/02_Features/Reference MEP Coverage.md
autonomous: true
requirements: [QUICK-260916-8l8]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - A service layer newly published above 300 draw calls fails the test suite, not warns.
    - A layer already waived fails the moment its draw calls exceed the figure recorded in its waiver.
    - Deleting a waiver entry re-arms the 300 ceiling for that layer.
    - Nothing currently published changes: the 21 live service layers pass unmodified.
    - The failure path has been observed firing, driven directly, not inferred from a green roster.
    - A manifest that misstates its own drawCalls cannot satisfy the budget, because the GLB is recounted.
  artifacts:
    - src/lib/reference-buildings/service-layer-budget.ts
    - src/lib/reference-buildings/__tests__/service-layer-budget.test.ts
    - src/lib/reference-buildings/__tests__/service-layer-budget-failure.test.ts
    - src/lib/reference-buildings/__tests__/service-layer-budget.glb.test.ts
  key_links:
    - The waiver register and the live manifest roster are cross-checked against each other, so a waiver cannot exist for an under-budget layer and an over-budget layer cannot exist without one.
    - The GLB recount feeds the SAME check function as the manifest figure, so both arms share one ceiling and one waiver register.
---

<objective>
Give reference-building **service layers** the draw-call budget they currently
lack. Detail layers assert `<= 200` and material layers assert `<= 300`;
service/MEP layers assert nothing, which is how Sixty5 `plumbing.glb` shipped
needing 2,462 draw calls with no build-time complaint.

Purpose: a hard 300 ceiling with today's five breaches recorded as dated,
named, reversible waivers carrying the measured figure and a reason — so a
breach is visible and named rather than silent, and is never fixed by quietly
raising the ceiling.

Output: one pure TS module holding the ceiling, the waiver register and the
check; one manifest-level test that runs it over the live roster; one GLB-level
test that recounts draw calls out of the published binaries and feeds the
recount through the same check; one paragraph in the feature doc.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@CLAUDE.md

Read before writing — these are the patterns to mirror, not to reinvent:
@src/lib/reference-buildings/__tests__/material-fabric.test.ts
@src/lib/reference-buildings/__tests__/mep-coverage.test.ts
@src/components/reference-building/__tests__/reference-architectural-details.test.tsx
@scripts/check-asset-budget.mjs
</context>

<ground_truth>
Measured at planning time from the eleven live
`public/reference-buildings/*/manifest.json` files at HEAD `b3bf4d1`, and
independently re-derived during planning. These are facts; do not re-guess
them, but DO read each manifest before authoring its waiver text.

**21 service layers across 6 publishing models; 5 models publish none.**

> The brief that commissioned this work said "21 service layers exist across 5
> models (the other 6 models publish none)". Its table was right and that
> sentence was transposed: `bs-medical-dental-clinic` 3, `duplex-apartment` 3,
> `schependomlaan` 7, `sixty5` 3, `taltech-maemaja` 1,
> `west-riverside-hospital` 4 = 21 layers from 6 models; the 5 with none are
> `fzk-haus`, `kit-office`, `klassiqua-office-1970`, `tum-fantasy-hotel-1`,
> `tum-fantasy-hotel-2`. Exactly the AGENTS.md failure this plan is guarding
> against, in the brief for the guard. Use 6 and 5 in that order.

Exactly five layers exceed 300 and therefore need waiver entries. Every integer
below was read from the manifest row named:

| key | drawCalls | elements | triangleCount | distinctGeometries | instancedShapes | instancedPlacements |
|---|---|---|---|---|---|---|
| `sixty5/plumbing` | 2462 | 22596 | 1028881 | 14980 | 2456 | 59312 |
| `sixty5/electrical` | 824 | 19604 | 460015 | 8692 | 820 | 68662 |
| `sixty5/hvac` | 750 | 11948 | 707714 | 6477 | 745 | 32230 |
| `west-riverside-hospital/hvac` | 737 | 19670 | 681115 | 10020 | 733 | 14403 |
| `bs-medical-dental-clinic/plumbing` | 407 | 6587 | 340774 | 3681 | 402 | 7872 |

`bs-medical-dental-clinic/hvac` is 276 and `west-riverside-hospital/sprinkler`
is 249 — both UNDER 300, so neither gets a waiver, and both are close enough
that the guard bites them if they grow. That is intended, and Task 2 proves it.

Service-layer rows carry NO `sha256` field (unlike `architecturalDetails` and
`materialFabric`), so the GLB arm verifies size and recounts draw calls; it
cannot hash-verify. Do not add a hash assertion.

All 21 GLBs total 306.30 MB and are present on disk in this working tree.
</ground_truth>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Ceiling, waiver register and the live-roster check, end to end</name>
  <files>src/lib/reference-buildings/service-layer-budget.ts, src/lib/reference-buildings/__tests__/service-layer-budget.test.ts</files>
  <read_first>
    src/lib/reference-buildings/__tests__/material-fabric.test.ts (lines 1-40 — the `NO_MATERIAL_FABRIC` register: a `Record` keyed by the thing being excused, a prose reason per entry, and the doc comment that calls an entry "a decision on the record, not an exemption to reach for". Mirror that voice.)
    scripts/check-asset-budget.mjs (lines 1-45 — the header comment where every rule names the actual failure that caused it, `LIMITS` with "Raise deliberately, with a reason", and `KNOWN_OVERSIZE` as "a debt record, not an exemption — delete it when fixed". This is the house style for this kind of file.)
    src/lib/reference-buildings/manifest.ts (lines 316-352 — the `serviceLayers` shape and the doc comment stating that `drawCalls` "is what decides whether the layer is usable" and that an instanced shape costs one draw call however often it is placed. Quote that framing in the module's own doc comment.)
    Each of the five breaching manifests under public/reference-buildings/, before writing that layer's waiver reason.
  </read_first>
  <behavior>
    Drive these through the exported check, with the real roster:
    - The 21 live rows plus the shipped waiver register produce ZERO findings.
    - Exactly 21 rows are collected, from exactly 6 publishing models; the 5 non-publishing models contribute none and are not silently skipped (the collected count is asserted, so a layer that drops out fails here).
    - The set of waiver keys equals exactly the set of live keys whose drawCalls exceed 300 — so a waiver for an under-budget layer and an un-waived breach are both impossible.
    - Every waiver's `measuredDrawCalls` is above 300 and its `measuredOn` parses as a date.
    - Every integer appearing in a waiver's prose reason is one of that layer's own manifest integer fields, the budget 300, or the overage (drawCalls - 300). No other digits are permitted in the prose.
  </behavior>
  <action>
Create `src/lib/reference-buildings/service-layer-budget.ts` as a pure module with
no filesystem, Three.js or Next imports, so both test arms and any future caller
share one ceiling and one register.

Export `SERVICE_LAYER_DRAW_CALL_BUDGET = 300`, with a doc comment stating it is
the same ceiling the material fabric already holds, and that the material-fabric
register's stance carries over verbatim: the budget is a render-performance
guard, so a building goes without the layer rather than the guard being raised
for it. Name the waiver mechanism as the alternative to raising it.

Export a `ServiceLayerWaiver` shape carrying, per entry: `measuredDrawCalls`
(the figure read at the date below — growth past it fails), `measuredOn`
(`YYYY-MM-DD`), and `reason` (prose). Export
`SERVICE_LAYER_BUDGET_WAIVERS` as a `Record<string, ServiceLayerWaiver>` keyed
`` `${buildingId}/${layerId}` ``, holding exactly the five entries in
`<ground_truth>`, each `measuredOn` set to `2026-09-16`. The register's doc
comment must say that an entry is a debt record, that deleting an entry re-arms
the 300 ceiling for that layer, and that the register is never the way to admit
a new layer.

Write each reason from the manifest row you just read. State what the figure is
and why the layer ships anyway: these five were published before any
service-layer ceiling existed and are live in production, so the ceiling lands
as a waiver rather than as the removal of shipped MEP geometry. Cite the
layer's packing — `distinctGeometries`, `instancedShapes`, `instancedPlacements`
— because that is what explains the draw-call count, and the manifest's own doc
comment says an instanced shape costs one call however often it is placed.
Constraint, from the ten label-lies instances in AGENTS.md: every integer in the
prose must be one you actually read from that row, or 300, or the overage; any
subtraction you write must compute. The test in `<behavior>` enforces exactly
this, so a reason with an unsourced number fails rather than ships.

Export `collectServiceLayerRows(manifests)` taking
`{ buildingId, serviceLayers }`-shaped input and returning flat rows of
`{ key, buildingId, layerId, drawCalls, elements, ... }`, treating an absent
`serviceLayers` as zero rows.

Export `checkServiceLayerBudget(rows, waivers = SERVICE_LAYER_BUDGET_WAIVERS)`
returning `{ key, kind, message }[]` — empty means pass. Four kinds:
  - `over_budget` — no waiver and `drawCalls > 300`.
  - `waiver_grown` — a waiver exists and `drawCalls > waiver.measuredDrawCalls`.
  - `stale_waiver` — a waiver exists and `drawCalls <= 300`; the layer no longer
    needs excusing, so the entry must be deleted to re-arm the guard.
  - `orphan_waiver` — a waiver whose key matches no collected row.
A waiver whose layer sits above 300 but at or below the recorded figure produces
nothing: that is the shipped state, unchanged.

Compute every figure in a `message` from the row rather than writing it by hand,
including the overage, so the sentence cannot drift from the number. Phrase a
waived layer's message as "measured N on DATE", never as an assertion that the
current count still equals N. An `orphan_waiver` message must say the key
matched no published layer — it must not read as a claim about that layer's
draw calls, because absent is not false.

Then create `src/lib/reference-buildings/__tests__/service-layer-budget.test.ts`.
It reads ONLY `public/reference-buildings/<id>/manifest.json` for each
`REFERENCE_BUILDING_IDS` entry — no GLB bytes, so this arm runs in a fresh
clone and in CI where the 52 reference GLBs live in the Blob store and not on
disk. Assert every item in `<behavior>`.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run src/lib/reference-buildings/__tests__/service-layer-budget.test.ts</automated>
  </verify>
  <done>The module exists with the 300 ceiling and exactly five waivers; the manifest-level test passes against the live roster with zero findings; 21 rows from 6 models are asserted; the waiver key set is proven equal to the over-300 key set; every digit in every waiver reason is traceable to a manifest field, 300, or the overage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the guard bites — drive the failure path directly</name>
  <files>src/lib/reference-buildings/__tests__/service-layer-budget-failure.test.ts</files>
  <behavior>
    Synthetic rows through the real `checkServiceLayerBudget`, asserting the KIND and the message content, not merely that something failed:
    - A new layer at 301 with no waiver yields exactly one `over_budget`.
    - A new layer at exactly 300 yields nothing — the ceiling is inclusive.
    - A waived layer at `measuredDrawCalls + 1` yields `waiver_grown`; at exactly `measuredDrawCalls` it yields nothing.
    - A waived layer that has fallen to 300 yields `stale_waiver`, and the message says the entry must be deleted.
    - A waiver key matching no row yields `orphan_waiver`, and its message makes no claim about that layer's draw-call count.
    - Removing `sixty5/plumbing` from the waiver map re-arms the ceiling: the same 2462 row now yields `over_budget`, where with the waiver present it yielded nothing. Both halves asserted in one test, so the re-arm is demonstrated rather than assumed.
    - `bs-medical-dental-clinic/hvac` at its live 276 yields nothing; the same key at 301 yields `over_budget` — the near-budget layers are armed.
    - Message arithmetic reproduces: parse the overage back out of an `over_budget` message and assert it equals `drawCalls - 300`; parse the recorded figure back out of a `waiver_grown` message and assert it equals that waiver's `measuredDrawCalls`.
  </behavior>
  <action>
Import the real `checkServiceLayerBudget`, `SERVICE_LAYER_DRAW_CALL_BUDGET` and
`SERVICE_LAYER_BUDGET_WAIVERS` and drive them with synthetic rows. Do not
perturb any manifest on disk and do not mutate the exported register in place —
build a modified copy for the re-arm case, so a failed run cannot leave the
shipped register altered in a checkout shared with other sessions.

Every case in `<behavior>` asserts the finding `kind` and, where stated, parses
the figure back out of the `message` and checks it reproduces. A guard that has
only ever been seen passing is not known to work; a message that only ever
appears beside a correct number is not known to say the right thing. Both are
the point of this task — AGENTS.md records ten instances in this repo where the
arithmetic was right and the sentence beside it was false, including one where a
verification pass checked the values and the prose built from them went out
unchecked.

Open the file with a short comment saying why it exists: the sibling test in
Task 1 only ever observes the roster passing, so the failure path is exercised
here instead.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run src/lib/reference-buildings/__tests__/service-layer-budget-failure.test.ts</automated>
  </verify>
  <done>All nine cases pass; each asserts a finding kind; the re-arm case shows the same 2462 row passing with the waiver and failing without it in one test; overage and recorded-figure arithmetic are parsed back out of the messages and reproduce.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Recount draw calls out of the published GLBs, and document the guard</name>
  <files>src/lib/reference-buildings/__tests__/service-layer-budget.glb.test.ts, docs/02_Features/Reference MEP Coverage.md</files>
  <precondition>The 52 reference-building GLBs are present under `public/reference-buildings/` — they live in the `bim-reference-glbs` Blob store and are excluded by `.gitignore:91`, so a fresh checkout needs `node scripts/restore-reference-glbs.mjs` first. All 21 service-layer GLBs (306.30 MB) were verified present in this working tree at planning time.</precondition>
  <read_first>
    src/components/reference-building/__tests__/reference-architectural-details.test.tsx (lines 58-70 — the glTF-binary header technique: JSON chunk length at byte offset 12, JSON from offset 20, then `nodes.reduce(...)` over `meshes[node.mesh].primitives.length`. This is the pattern that catches a manifest lying about its own figure. Reuse the recount expression.)
    src/components/reference-building/__tests__/reference-retrofit-visuals.glb.test.ts (lines 1-12 and 84-92 — why a `.glb.test.ts` file exists as its own thing, and the established header/`existsSync` handling.)
    docs/02_Features/Reference MEP Coverage.md (the `## Build and checks` section and its closing paragraphs — match that register.)
  </read_first>
  <behavior>
    - For each of the 21 published service layers: the draw calls recounted from the GLB equal the manifest's `drawCalls`.
    - The recounted figures, fed through the SAME `checkServiceLayerBudget` and the SAME waiver register, produce zero findings — so a manifest that understates its own count cannot satisfy the budget.
    - `statSync(...).size` equals the manifest `byteLength`.
    - A layer whose `file` is not a plain `.glb` basename fails with a message naming the offending value.
    - A missing GLB fails with a message that names `scripts/restore-reference-glbs.mjs` and does not read as a budget breach.
  </behavior>
  <action>
Create `src/lib/reference-buildings/__tests__/service-layer-budget.glb.test.ts`,
following the existing `.glb.test.ts` naming convention for a test that reads
the real published binaries.

Read the glTF JSON chunk only: open the file, take the 12-byte header, read the
JSON chunk length at offset 12, then read exactly that many bytes from offset
20. Do not `readFileSync` a service-layer GLB — the 21 files total 306.30 MB and
the recount needs the JSON chunk alone. Take the total size from
`statSync(...).size`. Recount with the precedent's expression:
`nodes.reduce((sum, node) => sum + (node.mesh === undefined ? 0 : meshes[node.mesh].primitives.length), 0)`.

Before joining a manifest `file` onto the building directory, reject any value
that is not a plain basename ending `.glb` — no path separators, no parent
segments — and fail naming the value. These paths come out of JSON on disk, and
the check should not be the thing that reads outside `public/reference-buildings/`.

When a GLB is absent, fail with a message naming
`scripts/restore-reference-glbs.mjs`, not a raw ENOENT and not a skip. This is
the deliberate decision on constraint (1): the four sibling tests
(`architectural-details`, `material-fabric`, `mep-coverage`,
`reference-architectural-details`) already read these bytes unconditionally, so
the restore is already a precondition of the suite and this arm adds no new
download — it only reads a few KB per file where the siblings read whole GLBs. A
silent skip would make the guard capable of not running, which is the absence-is-
not-false trap; and skipping a failing test is disallowed outright. Say so in a
comment at the top of the file.

Then add one short paragraph to the `## Build and checks` section of
`docs/02_Features/Reference MEP Coverage.md`, in the register of the paragraphs
already there: service layers now hold a 300 draw-call ceiling, the five
pre-existing breaches are named waivers carrying their measured figure and the
date, deleting an entry re-arms the ceiling, and the GLB arm recounts so a
manifest cannot vouch for itself. State the two arms by filename. Do not restate
the per-layer figures in the doc — they live in the register, and a second copy
is a second thing to go stale.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run src/lib/reference-buildings/__tests__/service-layer-budget.glb.test.ts</automated>
  </verify>
  <done>All 21 layers recount to their manifest figure; the recounted roster passes the same budget check with zero findings; sizes match; the basename guard and the absent-GLB message are both asserted; the feature doc carries one paragraph naming both arms.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo file tree → test process | `manifest.json` values and GLB bytes under `public/reference-buildings/` are read and their `file` fields are joined onto a directory path |
| waiver register (source) → guard verdict | the register decides which breaches are tolerated, so an edit to it changes what the suite permits |

## STRIDE Threat Register (ASVS L1, blocking on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8l8-01 | Tampering | `SERVICE_LAYER_BUDGET_WAIVERS` | low | mitigate | The register is source-controlled TS visible in the diff, and Task 1 asserts the waiver key set equals the live over-300 key set — so adding a waiver to silence an under-budget layer FAILS as `stale_waiver` rather than passing quietly. Silencing the guard cannot be free or invisible. |
| T-8l8-02 | Denial of Service | GLB reads in Task 3 | low | mitigate | Read the glTF JSON chunk only (header + declared JSON length); size via `statSync`. Never buffer a service-layer GLB, whose 21 files total 306.30 MB. |
| T-8l8-03 | Tampering | manifest `file` → directory join | low | mitigate | Reject any `file` that is not a plain `.glb` basename before joining, failing with the offending value named. Keeps the check inside `public/reference-buildings/`. |
| T-8l8-04 | Information Disclosure | everything read | low | accept | Every byte read is already published under `public/` and already served from production through the rewrite. The guard exposes nothing new. |
| T-8l8-SC | Tampering | npm/pip/cargo installs | low | accept | No package install and no new dependency in this task — nothing to audit, so no legitimacy checkpoint applies. |

No high-severity threat: nothing here blocks.
</threat_model>

<verification>
Run all three, from the repo root, exactly as AGENTS.md specifies (bare `pnpm`
fails on this machine and `pnpm exec` attempts to purge `node_modules`):

```
node node_modules/typescript/bin/tsc --noEmit
node node_modules/vitest/vitest.mjs run src/lib/reference-buildings src/components/reference-building
node node_modules/eslint/bin/eslint.js src/lib/reference-buildings
```

Then the full suite once, to confirm nothing that reads `serviceLayers` regressed:

```
node node_modules/vitest/vitest.mjs run
```

Report the real counts. Do not pipe a check through a pager and read the
pager's exit status — `tsc | tail && echo $?` reports `tail`'s status, and the
readings stay plausible, so nothing ever forces the discovery.

Then demonstrate the guard against the live roster once, by hand, and undo it:
temporarily raise one manifest's `drawCalls` (e.g. `bs-medical-dental-clinic`
`hvac`, 276 → 301), confirm `service-layer-budget.test.ts` FAILS with
`over_budget`, then `git checkout --` that one manifest path and confirm the
test returns to green. This is the roster-level counterpart to Task 2's
synthetic drive: it proves the wiring from real file to real failure, not just
the function.
</verification>

<commit_discipline>
This checkout is shared by several concurrent Claude sessions and the working
tree already carries unrelated modified/untracked runtime artifacts
(`.claude/settings.local.json`, `.planning/config.json`, `.planning/state.json`,
`.planning/milestone.lock`, `.gsd/`). Stage explicit paths only:

```
git add src/lib/reference-buildings/service-layer-budget.ts \
        src/lib/reference-buildings/__tests__/service-layer-budget.test.ts \
        src/lib/reference-buildings/__tests__/service-layer-budget-failure.test.ts \
        src/lib/reference-buildings/__tests__/service-layer-budget.glb.test.ts \
        "docs/02_Features/Reference MEP Coverage.md"
```

Never `git commit -a`, `git add -A` or `git add .`. Stay on
`feat/design-stage-energy-diagnostics`; do not branch. Verify the commit author
is `namseunghun97@gmail.com` before any deploy — a foreign author email makes
Vercel report `BLOCKED`, which is not a build failure.
</commit_discipline>

<success_criteria>
- A service layer published above 300 draw calls without a waiver fails the
  suite; observed firing, both synthetically (Task 2) and against a real
  manifest (verification).
- A waived layer fails as soon as its count exceeds the figure in its waiver.
- Deleting a waiver entry re-arms the ceiling for that layer; proven in one test
  that shows the same row passing with the entry and failing without it.
- A waiver cannot exist for a layer under 300, and no layer over 300 can exist
  without one; both enforced by set equality against the live roster.
- The 21 live service layers across 6 models pass unchanged. No manifest, no
  GLB and no shipped MEP geometry is modified by this task.
- The GLB recount agrees with all 21 manifest figures and satisfies the same
  ceiling, so a manifest cannot vouch for its own count.
- Every integer in every waiver reason is traceable to that layer's manifest
  row, to 300, or to the overage; enforced by test, not by review.
- `tsc --noEmit`, the full vitest run and eslint over
  `src/lib/reference-buildings` are all clean, run directly via `node`.
</success_criteria>

<output>
Create `.planning/quick/260916-8l8-add-a-draw-call-budget-to-reference-buil/260916-8l8-SUMMARY.md` when done.
</output>
