---
phase: 260916-0bz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - .gitignore
  - .vercelignore
  - e2e/model-anchors.spec.ts
  - scripts/check-asset-budget.mjs
  - scripts/publish-reference-glbs.mjs
  - scripts/restore-reference-glbs.mjs
autonomous: false
requirements: [URL-01, URL-02, BUILD-01, BUILD-02, DISK-01, PUB-01]
user_setup:
  - service: vercel-blob
    why: "Public object storage for the 52 reference GLBs (470.12 MB). Region and access are immutable once the store is created, so creation is a human checkpoint."
    env_vars:
      - name: BLOB_PUBLIC_BASE_URL
        source: "Vercel Dashboard -> bim -> Storage -> new store; value is https://<store-id>.public.blob.vercel-storage.com (identifier, not a secret)"
      - name: BLOB_READ_WRITE_TOKEN
        source: "vercel env pull .env.local after the store is connected; never commit, never NEXT_PUBLIC_"
    dashboard_config:
      - task: "Create a PUBLIC Blob store on project bim, region icn1, connect it to Production + Preview + Development"
        location: "Vercel Dashboard -> matts-projects-d0677dc4/bim -> Storage (or: vercel blob create-store <name> --access public --region icn1 --yes --scope matts-projects-d0677dc4)"
      - task: "Disable Enhanced Build Machines only when the Task 4 checkpoint asks for it"
        location: "Vercel Dashboard -> bim -> Settings -> Build"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "Every published GLB URL (/reference-buildings/:id/:file.glb) returns 200 with glTF magic in production, including Sixty5 plumbing.glb (69,730,700 bytes) — served through the blob rewrite, not from public/"
    - "git ls-files public/reference-buildings shows zero .glb files — 470.12 MB (492,954,436 bytes, 52 files) is out of the Vercel build checkout"
    - "A clean build completes on the standard 4-core/8 GB builder with Enhanced Build Machines disabled (the proof; the setting is not turned off before that evidence exists)"
    - "X-Vercel-Id on the production plumbing.glb response still resolves to compute region icn1"
    - "A clean checkout restores all 52 GLBs onto disk by unauthenticated HTTPS and the four readFileSync test files (lossless-materials, pv-bearing-source, reference-retrofit-visuals.glb, reference-architectural-details) pass with assertions unweakened"
    - "Manifests, datasets, flow graphs, SVG, spaces/openings/roof-planes JSON, licences, textures, HDR and wasm stay in git and keep serving from public/"
  artifacts:
    - "next.config.ts rewrites() gated on BLOB_PUBLIC_BASE_URL (afterFiles default; inert when unset)"
    - "scripts/publish-reference-glbs.mjs (vercel blob put, pathname preserved, no random suffix, allowOverwrite)"
    - "scripts/restore-reference-glbs.mjs (unauthenticated public HTTPS GET back onto disk, byteLength-checked against manifests)"
    - "scripts/check-asset-budget.mjs extended: tracked-GLB error + VERCEL=1 on-disk GLB error"
    - ".gitignore glob public/reference-buildings/**/*.glb; .vercelignore same glob (migration commit only)"
    - "e2e/model-anchors.spec.ts enumerating every published GLB URL from the served manifests"
    - "External: public Blob store on bim (icn1) holding exactly the 52 reference-buildings/** pathnames"
  key_links:
    - "BLOB_PUBLIC_BASE_URL -> next.config.ts rewrites() destination (hostname exists only after the Task 3 checkpoint)"
    - "Blob pathname === URL path minus the leading slash; addRandomSuffix stays false or every rewrite and e2e GET 404s"
    - ".vercelignore glob lands ONLY in the Task 5 migration commit — landing it earlier makes any interim CLI deploy drop the GLBs while no rewrite is live yet (production 404s)"
    - "Restore script integrity <- manifest byteLength fields already in git (no token needed)"

assumption_delta_decision:
  noun: "Where reference-building GLB bytes live — was: git-tracked files under public/reference-buildings/; now: objects in the bim public Blob store, with disk copies under public/ demoted to an ephemeral local cache"
  decision: promote
  rationale: "After git rm the blob object is the only durable copy of each GLB; restore-reference-glbs.mjs re-materializes the cache on demand for next dev / Playwright / vitest."
---

<objective>
Move the 52 published reference-building GLBs (470.12 MB) out of `public/reference-buildings/` into a new public Vercel Blob store on project `bim` (region `icn1`), keeping the exact public URL contract `/reference-buildings/:id/:file.glb` via a Next.js external rewrite, so the standard 8 GB builder can build the app again and model twelve will not hit a third Vercel ceiling.

Purpose: Two ceilings already failed by actual deployment — function tracing hit 296.3 MB against 250 MB (fixed by `outputFileTracingExcludes`), then the 4-core/8 GB builder OOM'd twice (masked by Enhanced Build Machines, which is load-bearing). The cause is 470 MB of committed meshes in the checkout; this task removes the cause.
Output: Blob store with 52 objects, rewrite + ignore globs + budget guards, publish/restore operator scripts, extended e2e coverage, and production evidence that the URL contract holds on the standard builder.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260916-0bz-move-published-reference-building-glbs-o/260916-0bz-CONTEXT.md
@.planning/quick/260916-0bz-move-published-reference-building-glbs-o/260916-0bz-RESEARCH.md
@next.config.ts
@scripts/check-asset-budget.mjs
@e2e/model-anchors.spec.ts

Live planning-time observations (2026-09-16, this machine):
- 52 GLBs on disk, 492,954,436 bytes, all 52 git-tracked; largest `sixty5/plumbing.glb` 69,730,700 bytes.
- 11 building ids in `src/lib/reference-buildings/registry.json`; each served `manifest.json` references its GLBs at `model.file`, `serviceLayers[].file`, `architecturalDetails.file` — all plain filenames, no directories.
- `check:assets` is a manual npm script, NOT wired into `next build` — new budget guards cannot break a Vercel build, but a tracked-GLB guard landed before the `git rm` would fail locally; sequence accordingly (Task 5).
- Playwright supports `E2E_BASE_URL` for runs against production; its managed webServer is `next dev`, which does not apply `vercel.json` rewrites — the rewrite belongs in `next.config.ts` only.
</context>

<capability_coverage>
External capability surface (Vercel Blob + Next.js delivery) — INTEGRATE or OPT-OUT per capability:

| Capability | Disposition | Reason |
|------------|-------------|--------|
| `vercel blob create-store` (public, icn1) | INTEGRATE | Hosting is locked to a new public store on bim; region/access immutable → human checkpoint (Task 3) |
| `vercel blob put` with `--pathname`, `--access public`, `--content-type model/gltf-binary`, `--allow-overwrite`, no random suffix | INTEGRATE | The upload mechanism; pathname preservation is the URL contract (Task 2 script, run in Task 5) |
| `vercel blob list --prefix reference-buildings/` | INTEGRATE | PUB-01 store-vs-manifest inventory verification after upload (Task 5) |
| Unauthenticated public HTTPS GET of blob URLs | INTEGRATE | Restore script for next dev / Playwright / vitest; no token on the read path (Task 2) |
| Next.js `rewrites()` external destination, default afterFiles order | INTEGRATE | The URL contract; filesystem wins locally, rewrite fires in production after git rm (Task 1) |
| Multipart upload of the 69.7 MB canary | INTEGRATE (CLI default) | CLI handles chunking/retries; do not hand-roll |
| `@vercel/blob` SDK in the Next bundle | OPT-OUT | CLI-first; the app runtime gains zero dependencies (locked). DevDependency only if the CLI proves insufficient |
| Route-handler proxy (`get()` through a function) | OPT-OUT | Streams 69.7 MB through a function — reintroduces the duration/memory/transfer failure this task removes |
| Private store / signed URLs | OPT-OUT | These meshes are already world-readable; private would force the function proxy above |
| `vercel.json` rewrites | OPT-OUT | `next dev` (Playwright's webServer) does not apply them; vercel.json stays `{ regions: ["icn1"] }` only |
| `beforeFiles` rewrite order | OPT-OUT | Would shadow restored local files and always hit Blob, breaking the local-restore contract |
| `addRandomSuffix` | OPT-OUT | Suffix breaks the stable-pathname contract; every rewrite and e2e GET would 404 |
| R2/S3 second cloud | OPT-OUT | Locked: only if Blob cannot serve these sizes; max file 5 TB and CDN cache 512 MB cover 69.7 MB |
| Blob WAF / rate limiting | OPT-OUT | Research-deferred; guessable public URLs are required by the path contract and already public today |
| Git history rewrite (filter-repo/BFG) | OPT-OUT | Explicitly out of scope; build memory is this task, history size is a separate problem |
</capability_coverage>

<tasks>

<task type="auto">
  <name>Task 1: URL contract rewrite, gitignore glob, and full-GLB e2e coverage</name>
  <files>next.config.ts, .gitignore, e2e/model-anchors.spec.ts</files>
  <reversibility rating="reversible">Config is inert without the env var; ignore glob does not untrack; test changes are additive.</reversibility>
  <action>
    In `next.config.ts` add an async `rewrites()` that reads `process.env.BLOB_PUBLIC_BASE_URL`, strips any trailing slash, and returns an empty array when the variable is unset (inert locally and in any environment where the store is not wired yet). When set, return the default array form (which is afterFiles order: `public/` files win when present, so restored local GLBs and the pre-migration tree keep serving from disk) with exactly one entry: source `/reference-buildings/:id/:file([^/]+\\.glb)`, destination `${blobBase}/reference-buildings/:id/:file`. The `.glb`-only constraint is deliberate (open-proxy defense; JSON/SVG/licences stay on `public/`). Do NOT touch `outputFileTracingIncludes`/`Excludes` (the GLB exclude stays — it is not a substitute for the git rm, and removing it re-traces meshes into functions). Do NOT add anything to `vercel.json` — it stays `{ "regions": ["icn1"] }`, and `next dev` would not apply vercel.json rewrites to Playwright anyway. Update the stale comment above the excludes that calls static hosting "the CDN" so it names the rewrite as the post-migration delivery path.

    In `.gitignore` add `public/reference-buildings/**/*.glb` with a comment that the bytes live in the bim Blob store and `scripts/restore-reference-glbs.mjs` re-materializes them. Do NOT add the glob to `.vercelignore` in this task — `.vercelignore` filters the CLI upload, so landing it before the migration commit would make any interim `vercel --prod` drop the GLBs while no rewrite destination exists yet, 404ing every model in production. That glob lands in Task 5's migration commit.

    In `e2e/model-anchors.spec.ts` keep the two existing hotel tests byte-for-byte intact and add one new test: read the id list from `src/lib/reference-buildings/registry.json` via `fs.readFileSync` + `JSON.parse` (NOT a JSON import — tsconfig/Playwright transform risk), then for each id `page.request.get` the served `/reference-buildings/${id}/manifest.json`, recursively walk the parsed JSON collecting string values ending in `.glb`, assert each collected value matches `/^[^/]+\.glb$/` (a future manifest carrying a path must fail loudly, not fetch a wrong URL), dedupe, and assert the total is at least 52 (floor; model twelve raises it). GET every collected URL `/reference-buildings/${id}/${file}` and assert `response.ok()` and first four bytes `glTF`. This covers every published layer GLB — Sixty5 `plumbing.glb` (69.7 MB) is the size canary — and works against both a restored local tree (public/ wins) and production (rewrite fires).
  </action>
  <verify>
    <automated>node node_modules/typescript/bin/tsc --noEmit &amp;&amp; node scripts/check-asset-budget.mjs &amp;&amp; node node_modules/@playwright/test/cli.js test e2e/model-anchors.spec.ts --list &amp;&amp; node -e "const fs=require('fs'),path=require('path');const ids=Object.keys(JSON.parse(fs.readFileSync('src/lib/reference-buildings/registry.json','utf8')));let n=0;const walk=o=>{if(!o||typeof o!=='object')return;for(const v of Object.values(o)){if(typeof v==='string'&&v.endsWith('.glb'))n++;else if(typeof v==='object')walk(v)}};for(const id of ids){walk(JSON.parse(fs.readFileSync(path.join('public/reference-buildings',id,'manifest.json'),'utf8')))}if(n!==52){console.error('expected 52 manifest GLBs, got',n);process.exit(1)}console.log('manifest GLB inventory:',n)"</automated>
  </verify>
  <done>tsc clean; asset budget still green with the 52 GLBs tracked (gitignore does not untrack); Playwright lists the extended spec without a dev server; the manifest-walk gate prints exactly 52, proving the enumeration the new e2e test uses. The rewrite is committed but inert everywhere until BLOB_PUBLIC_BASE_URL exists.</done>
</task>

<task type="auto">
  <name>Task 2: Publish and restore operator scripts (dry-run verifiable, no token needed to verify)</name>
  <files>scripts/publish-reference-glbs.mjs, scripts/restore-reference-glbs.mjs</files>
  <reversibility rating="reversible">New files; nothing invokes them until Task 5.</reversibility>
  <action>
    `scripts/publish-reference-glbs.mjs`: enumerate `*.glb` under `public/reference-buildings/` on disk; independently walk every `manifest.json` for its referenced `.glb` filenames (same recursive walk as the e2e gate); require the disk set and the manifest-referenced set to be exactly equal — a stray disk file or a manifest reference with no bytes fails the run before anything uploads. For each file, invoke the Vercel CLI: `vercel blob put <absolute path> --pathname reference-buildings/<id>/<file> --access public --content-type model/gltf-binary --allow-overwrite --scope matts-projects-d0677dc4`. NEVER pass `--add-random-suffix` (a suffixed pathname misses the rewrite and 404s e2e). Read `BLOB_READ_WRITE_TOKEN` from the process env, loading `.env.local` first when present (Node 24 `process.loadEnvFile`); never print the token; fail with instructions to run `vercel env pull .env.local` when it is absent on a real run. Support `--dry-run`: print each put command and the file count/byte total, exit non-zero unless the inventory is exactly the manifest-referenced set; dry-run requires no token and no env. If a CLI flag has drifted, correct it against `vercel blob put --help` and note the drift in the SUMMARY — the contract is pathname + public + overwrite + no suffix, not the exact flag spelling.

    `scripts/restore-reference-glbs.mjs`: for each `public/reference-buildings/*/manifest.json` (manifests stay in git, so this works on a clean checkout), walk for referenced `.glb` filenames and GET `&lt;base&gt;/reference-buildings/&lt;id&gt;/&lt;file&gt;` over unauthenticated public HTTPS — no token on the read path, ever. Base URL comes from `BLOB_PUBLIC_BASE_URL` (env or `.env.local`, trailing slash stripped); when absent on a real run, exit with the instruction to set it from the store created in Task 3. `mkdir -p` the target directory, skip files already on disk whose size matches the manifest `byteLength`, verify every downloaded byte count against the manifest `byteLength` where present and fail loudly on mismatch (the manifests in git are the integrity ledger). Support `--dry-run` printing the 52 pathnames and total bytes with no env required. End the real run with a summary line naming the four test files this unblocks: `lossless-materials.test.ts`, `pv-bearing-source.test.ts`, `reference-retrofit-visuals.glb.test.ts`, `reference-architectural-details.test.tsx` — their assertions stay unweakened; this script is their documented precondition.

    Both scripts are plain Node `.mjs`, no new dependencies (CLI-first; `@vercel/blob` stays out of the repo unless the CLI proves insufficient, and then devDependency only).
  </action>
  <verify>
    <automated>node scripts/publish-reference-glbs.mjs --dry-run &amp;&amp; node scripts/restore-reference-glbs.mjs --dry-run &amp;&amp; node -e "const {execFileSync}=require('child_process');const out=execFileSync('node',['scripts/publish-reference-glbs.mjs','--dry-run'],{encoding:'utf8'});if(!out.includes('reference-buildings/sixty5/plumbing.glb'))throw new Error('canary pathname missing');if(!/52/.test(out))throw new Error('inventory count missing');console.log('dry-run canary OK')"</automated>
  </verify>
  <done>Both dry-runs exit 0 on the current tree with no token and no env; publish dry-run prints 52 put commands including the exact pathname `reference-buildings/sixty5/plumbing.glb`; restore dry-run lists the same 52 pathnames; both scripts self-validate disk-vs-manifest inventory and byteLength integrity.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: CHECKPOINT — create the public Blob store on bim (icn1) and wire env vars</name>
  <what-built>Tasks 1-2 landed: the rewrite (inert until BLOB_PUBLIC_BASE_URL exists), the gitignore glob, full-GLB e2e coverage, and both operator scripts verified by dry-run. Nothing destructive has happened; all 52 GLBs are still tracked and serving from public/.</what-built>
  <why-checkpoint>Store region and access are immutable at creation, and the CLI default region is iad1 — a wrong choice here cannot be edited later, only deleted and recreated. The team already owns an empty iad1 store (`s2mas-blob`) attached to a different product; reusing it is explicitly rejected. The store hostname (`https://&lt;store-id&gt;.public.blob.vercel-storage.com`) does not exist until this step runs, and Tasks 4-5 depend on it.</why-checkpoint>
  <how-to-verify>
    1. Create the store: `vercel blob create-store &lt;name&gt; --access public --region icn1 --yes --scope matts-projects-d0677dc4` (or Vercel Dashboard → bim → Storage → Create → Public → icn1). Do NOT reuse `s2mas-blob`.
    2. Connect it to project `bim` for Production, Preview AND Development (the connect step provisions `BLOB_READ_WRITE_TOKEN` etc.).
    3. Add env var `BLOB_PUBLIC_BASE_URL=https://&lt;store-id&gt;.public.blob.vercel-storage.com` (no trailing slash) to Production, Preview and Development. It is an identifier, not a secret.
    4. Locally: `vercel env pull .env.local` (brings `BLOB_READ_WRITE_TOKEN`; `.env*` is gitignored) and append the same `BLOB_PUBLIC_BASE_URL` line.
    5. Confirm: `vercel blob list-stores --scope matts-projects-d0677dc4` shows the new store connected to bim.
  </how-to-verify>
  <resume-signal>User reports the store id and confirms `BLOB_PUBLIC_BASE_URL` is set in all three environments and in `.env.local`.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: CHECKPOINT — approve the upload and the 470 MB git rm (and flip the builder for the proof)</name>
  <what-built>Store exists and env vars are wired. Claude presents: the publish dry-run inventory (52 files, 492,954,436 bytes — exactly the manifest-referenced set), the exact `git rm` scope (`public/reference-buildings/**/*.glb` ONLY — manifests, datasets, flow graphs, SVG, JSON and licences stay in git), and the rollback story (blob upload is additive; the git rm is an ordinary revertible commit; the current production deployment keeps serving the GLBs as static files until a new deployment replaces it, so a failed migration never takes the gallery down).</what-built>
  <why-checkpoint>This is the point of no easy return for the checkout: 470 MB leaves git tracking and 470 MB is uploaded to a metered store. The user explicitly asked for a confirmation gate before both the blob upload (spends `BLOB_READ_WRITE_TOKEN` quota) and the `git rm`. yolo_mode is false.</why-checkpoint>
  <how-to-verify>
    1. Review the dry-run inventory and git rm scope Claude presents; confirm both.
    2. Confirm `vercel env pull .env.local` has run and `BLOB_READ_WRITE_TOKEN` is available locally (Task 3, step 4).
    3. In Vercel Dashboard → bim → Settings → Build, DISABLE Enhanced Build Machines now: Task 5's preview build then doubles as the locked success criterion — a clean build on the standard 4-core/8 GB builder. If that build OOMs, Claude stops, production is untouched, you re-enable the setting, and the task reassesses. Do not deploy anything else to bim between this toggle and Task 5 completing.
  </how-to-verify>
  <resume-signal>User replies with explicit approval for the upload AND the git rm, and confirms Enhanced Build Machines is off.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Publish, migrate, deploy from a clean worktree, and prove the evidence gate</name>
  <files>.vercelignore, scripts/check-asset-budget.mjs (plus git rm of the 52 public/reference-buildings/**/*.glb files)</files>
  <precondition>Tasks 3-4 confirmed: BLOB_PUBLIC_BASE_URL set in all three Vercel environments and .env.local; BLOB_READ_WRITE_TOKEN present locally; Enhanced Build Machines disabled; HEAD commit author email is namseunghun97@gmail.com (`git log -1 --format=%ae`) or the deploy is BLOCKED.</precondition>
  <reversibility rating="costly">Every step is individually reversible (additive upload, revertible commit, atomic deployments, previous production deployment retained), but the aggregate is the migration — rollback is revert + redeploy, not a no-op.</reversibility>
  <action>
    Execute in this exact order; stop and report at the first failure.

    1. Baseline: run the extended e2e locally with the GLBs still on disk (public/ wins over the inert rewrite): `node node_modules/@playwright/test/cli.js test e2e/model-anchors.spec.ts`. All green = the enumeration and the pre-migration contract are proven. Do not proceed on red.
    2. Upload: `node scripts/publish-reference-glbs.mjs` (real run; 52 puts, multipart handled by the CLI).
    3. Store inventory (PUB-01): `vercel blob list --prefix reference-buildings/ --scope matts-projects-d0677dc4` and compare against the manifest-referenced set — exactly 52 pathnames, each `reference-buildings/&lt;id&gt;/&lt;file&gt;` with no random suffix. A count or pathname mismatch means stop; do not git rm against an unproven store.
    4. Migration commit: `git rm` the 52 GLBs (only `*.glb` under `public/reference-buildings/`); add the `public/reference-buildings/**/*.glb` glob to `.vercelignore` (NOW it is safe — the rewrite destination exists and this commit removes the files); extend `scripts/check-asset-budget.mjs` with two new error-level guards: (a) any `.glb` under `public/reference-buildings/` in `git ls-files` output is an error (the bytes live in Blob now), (b) when `process.env.VERCEL === '1'`, any `.glb` on disk under that tree is an error (dirty-deploy OOM defense — restored local files must never reach a Vercel build). Verify `node scripts/check-asset-budget.mjs` exits 0 with the files gone and `git ls-files public/reference-buildings` filtered to `.glb` is empty. Commit with a conventional message (e.g. `chore(assets): move reference-building GLBs to Vercel Blob`). Do NOT rewrite git history — out of scope. Do NOT add a twelfth model.
    5. Clean detached worktree per AGENTS.md: `git worktree add --detach &lt;tmp&gt;/deploy HEAD`, copy `.vercel` into it, and deploy a PREVIEW from the worktree: `vercel deploy --yes --scope matts-projects-d0677dc4` (no `--prod`). The build running on the standard 8 GB builder (Enhanced off, Task 4) and completing IS the builder proof — capture the build output. OOM here means stop; production was never touched.
    6. Preview evidence: GET `https://&lt;preview&gt;/reference-buildings/sixty5/plumbing.glb` → 200 with first four bytes `glTF`; also GET `sixty5/architectural-details.glb` (proves the rewrite regex matches hyphenated names, research assumption A2) and one `model.glb` of another id.
    7. Production deploy from the same worktree: `vercel --prod --yes --scope matts-projects-d0677dc4`.
    8. Production evidence gate: GET `https://bim-self.vercel.app/reference-buildings/sixty5/plumbing.glb` → 200, `glTF` magic, and the `X-Vercel-Id` header's compute segment reading `icn1` (print the full header as evidence). Then run the whole extended spec against production: `E2E_BASE_URL=https://bim-self.vercel.app node node_modules/@playwright/test/cli.js test e2e/model-anchors.spec.ts` — every published GLB URL 200s through the rewrite.
    9. Local-restore proof (DISK-01): on the main checkout (GLBs now absent), `node scripts/restore-reference-glbs.mjs` restores all 52 by unauthenticated HTTPS with byteLength checks, then `node node_modules/vitest/vitest.mjs run src/lib/reference-buildings src/components/reference-building` passes with the four readFileSync test files' assertions unweakened.
    10. Push the migration commit. Report: the 8 GB builder proof exists, so Enhanced Build Machines may stay off — but leaving it off or re-enabling is the user's call, not this task's. If anything post-deploy fails: `git revert` the migration commit and redeploy; the blob objects are additive and harmless, and the prior production deployment is retained in the dashboard.
  </action>
  <verify>
    <automated>node -e "(async()=>{const r=await fetch('https://bim-self.vercel.app/reference-buildings/sixty5/plumbing.glb');const b=Buffer.from(await r.arrayBuffer());const id=r.headers.get('x-vercel-id')||'';console.log('status',r.status,'bytes',b.length,'x-vercel-id',id);if(r.status!==200)throw new Error('status '+r.status);if(b.subarray(0,4).toString()!=='glTF')throw new Error('bad magic');if(id.split('::')[1]!=='icn1')throw new Error('compute region not icn1: '+id);console.log('EVIDENCE GATE OK')})()"</automated>
  </verify>
  <done>Production GET of the Sixty5 plumbing.glb URL returns 200 with glTF magic and X-Vercel-Id compute region icn1; the extended e2e passes against production covering every published GLB; the preview/production builds completed on the standard 8 GB builder with Enhanced Build Machines off (build log captured); git ls-files shows zero tracked GLBs; check-asset-budget guards are live and green; restore + the four vitest files pass locally; migration commit pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator machine → Blob store | `BLOB_READ_WRITE_TOKEN` authorizes puts; write secret crossing from `.env.local` into CLI env |
| browser → Vercel edge → Blob origin | the rewrite is a reverse proxy; an over-broad source exposes the whole store namespace |
| public internet → Blob URLs | store is public by design; pathnames are guessable (required by the URL contract) |
| developer machine → public Blob URLs | restore path is unauthenticated; integrity comes from in-git manifest byteLength, not from auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0bz-01 | Information disclosure | token handling | high | mitigate | No `@vercel/blob` in `src/`, no `NEXT_PUBLIC_BLOB_*`, token only via `vercel env pull` → gitignored `.env.local`; scripts never print it; restore path is unauthenticated by design |
| T-0bz-02 | Tampering | publish script | medium | mitigate | Pathname derived from the on-disk relative path; `addRandomSuffix` never passed; disk-vs-manifest set equality required before any put; PUB-01 list-vs-manifest comparison before git rm |
| T-0bz-03 | Information disclosure | next.config.ts rewrite | low | mitigate | Source constrained to `/reference-buildings/:id/:file` ending `.glb`; fixed destination origin; JSON/SVG/licences never proxied |
| T-0bz-04 | Denial of service | public Blob URLs | low | accept | Guessable URLs are required by the path contract and the bytes are world-readable today; Blob WAF/rate-limit deferred per research |
| T-0bz-05 | Tampering | dirty deploy after local restore | high | mitigate | `.gitignore` + `.vercelignore` globs, `VERCEL=1` on-disk guard in check-asset-budget, and the AGENTS.md clean-detached-worktree deploy in Task 5 step 5/7 |
| T-0bz-SC | Tampering | supply chain | low | mitigate | CLI-first: no install task and no new runtime dependency, so no package-legitimacy checkpoint is required; `@vercel/blob` (audited OK: 4.8M dl/wk, vercel/storage repo) may enter as a devDependency only if the CLI proves insufficient |
</threat_model>

<verification>
- Task 1-2 gates: tsc, asset budget, Playwright `--list`, manifest-walk inventory = 52, both script dry-runs (all run pre-migration with GLBs in place).
- Task 3-4: human-verified store creation (icn1, public, connected ×3) and explicit approval of upload + git rm + builder toggle.
- Task 5 chain: local e2e green → 52 puts → store list == manifest set → git rm + guards green → preview build on the 8 GB builder → preview canary GETs → production deploy → production evidence gate (200 + glTF + icn1) → production e2e over every published GLB → restore + vitest green.
</verification>

<success_criteria>
The locked success criterion: a clean build on the standard 4-core/8 GB builder (Enhanced Build Machines off) with the 52 GLBs out of the checkout, while `https://bim-self.vercel.app/reference-buildings/sixty5/plumbing.glb` returns 200 with glTF magic from compute region icn1 — and every other published GLB URL behaves identically. Enhanced Build Machines stays on until that proof exists; turning it off permanently is the user's decision after the proof, not part of this task.
</success_criteria>

<output>
Create `.planning/quick/260916-0bz-move-published-reference-building-glbs-o/260916-0bz-01-SUMMARY.md` when done, recording: store id, the 52 uploaded pathnames, preview + production deploy URLs, the evidence-gate output (status, bytes, X-Vercel-Id), the 8 GB builder build result, and any CLI flag drift found during publish.
</output>
