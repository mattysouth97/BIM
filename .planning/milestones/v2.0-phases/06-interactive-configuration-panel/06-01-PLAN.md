---
phase: 06-interactive-configuration-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/recipe-store.ts
  - src/components/viewer/config-panel.tsx
  - src/components/viewer/building-scene.tsx
  - src/components/viewer/viewer-overlay.tsx
autonomous: true
---

# Plan 01: Recipe Override Store + ConfigPanel Shell

<objective>
Create a Zustand store for BuildingRecipe overrides and the ConfigPanel shell component with 4 tabs (Building, Envelope, Systems, Layers). Replace the MaterialPanel toggle in building-scene.tsx and viewer-overlay.tsx.
</objective>

<tasks>

<task id="1">
<title>Recipe override Zustand store</title>
<read_first>
- src/store/material-store.ts
- src/lib/procedural/types.ts
- src/lib/procedural/recipe.ts
</read_first>
<action>
Create `src/store/recipe-store.ts`:
- Import RecipeOverrides from @/lib/procedural/types
- Interface RecipeState:
  - overrides: Record&lt;string, RecipeOverrides&gt; (keyed by building PK)
  - setOverride(pk: string, path: string, value: unknown): void — deep-set into overrides[pk]
  - getOverrides(pk: string): RecipeOverrides | undefined
  - resetOverrides(pk: string): void — delete overrides[pk]
  - resetSection(pk: string, section: "facade" | "slab" | "column" | "roof"): void
- Create store with `create&lt;RecipeState&gt;()(...)` (non-persisted, like layer-store)
- The setOverride method should follow the same deep-clone + path-based pattern as material-store.overrideProperty
- Export as `useRecipeStore`
</action>
<acceptance_criteria>
- `src/store/recipe-store.ts` contains `export const useRecipeStore`
- Store has `setOverride`, `getOverrides`, `resetOverrides`, `resetSection` methods
- Overrides keyed by building PK string
- `pnpm build` passes
</acceptance_criteria>
</task>

<task id="2">
<title>ConfigPanel shell with 4 tabs</title>
<read_first>
- src/components/viewer/material-panel.tsx
- src/components/ui/tabs.tsx
- src/store/material-store.ts
- src/store/recipe-store.ts
</read_first>
<action>
Create `src/components/viewer/config-panel.tsx`:
- Props: `{ buildingPk: string; visible: boolean; onClose: () => void }`
- Uses shadcn Tabs component with 4 tabs: "building", "envelope", "systems", "layers"
- Tab labels (bilingual): Building/건물, Envelope/외피, Systems/설비, Layers/레이어
- Tab icons from lucide-react: Building2, Thermometer, Cog, Layers
- Panel container: `absolute top-3 left-3 z-20 w-96 max-h-[520px] overflow-y-auto rounded-lg border bg-card/95 backdrop-blur shadow-xl`
- Header with Settings icon, "Configuration" / "설정" title, source badge (from material-store), close button
- Each tab content renders a placeholder `<div>Tab content</div>` — Plan 02 fills these
- Copy sourceBadge helper from material-panel.tsx
- Export `ConfigPanel`
</action>
<acceptance_criteria>
- `src/components/viewer/config-panel.tsx` contains `export function ConfigPanel`
- Component uses shadcn Tabs with 4 tab triggers
- Tab values are "building", "envelope", "systems", "layers"
- Panel width is w-96
- Bilingual tab labels with isKo check
- `pnpm build` passes
</acceptance_criteria>
</task>

<task id="3">
<title>Replace MaterialPanel with ConfigPanel in scene</title>
<read_first>
- src/components/viewer/building-scene.tsx
- src/components/viewer/viewer-overlay.tsx
- src/components/viewer/config-panel.tsx
</read_first>
<action>
Update `src/components/viewer/building-scene.tsx`:
- Replace `import { MaterialPanel }` with `import { ConfigPanel }` from "./config-panel"
- Replace `materialPanelOpen` state → `configPanelOpen` state
- Replace `<MaterialPanel buildingPk={buildingPk} visible={materialPanelOpen} onClose={...} />` with `<ConfigPanel buildingPk={buildingPk} visible={configPanelOpen} onClose={...} />`
- Update ViewerOverlay props: `onToggleMaterialPanel` → `onToggleConfigPanel`, `materialPanelOpen` → `configPanelOpen`

Update `src/components/viewer/viewer-overlay.tsx`:
- Rename props: `onToggleMaterialPanel` → `onToggleConfigPanel`, `materialPanelOpen` → `configPanelOpen`
- Change Thermometer icon to Settings icon for the config button
- Update tooltip: "Material Properties" → "Configuration" / "설정"
</action>
<acceptance_criteria>
- building-scene.tsx imports ConfigPanel, not MaterialPanel
- building-scene.tsx state uses configPanelOpen
- viewer-overlay.tsx props use onToggleConfigPanel and configPanelOpen
- Settings icon used instead of Thermometer for config button
- `pnpm build` passes
</acceptance_criteria>
</task>

</tasks>

<verification>
- ConfigPanel renders with 4 visible tabs
- Config button in toolbar opens/closes the panel
- Recipe store can set and get overrides
- `pnpm build` passes
</verification>

<must_haves>
- useRecipeStore with path-based override system
- ConfigPanel with 4 tabs replacing MaterialPanel
- Building-scene and viewer-overlay updated to use ConfigPanel
</must_haves>
