---
phase: 17-panel-content-workflow-stepper
plan: 05
type: execute
wave: 3
depends_on: ["17-01", "17-02", "17-03", "17-04"]
files_modified:
  - src/components/workspace/workspace-shell.tsx
  - src/components/workspace/workflow-stepper.tsx
  - src/components/workspace/properties-panel.tsx
  - src/components/workspace/scene-outliner.tsx
  - src/components/workspace/component-catalog.tsx
  - src/hooks/use-undo-shortcut.ts
autonomous: false
requirements: [FLOW-01, CTX-01, DISC-01, DISC-02, UNDO-01, UNDO-02]

must_haves:
  truths:
    - "A 5-stage stepper (Select Building, Assemble, Configure, Analyze, Export) is visible at the top of the workspace; clicking a stage navigates to it without blocking access to other stages"
    - "User clicks a wall, room, or component in the 3D scene and the right dock panel immediately shows that element's editable properties"
    - "User can browse a tree outliner in the left dock showing floors, rooms, and placed components; clicking a tree node selects and highlights it in the 3D view"
    - "User can drag a door, window, MEP element, or stair from the component catalog in the left dock and drop it into the 3D scene"
    - "User presses Ctrl+Z and the last wall draw, component placement, or material edit is undone; pressing Ctrl+Y redoes it"
    - "User draws a wall that triggers auto room detection; pressing Ctrl+Z undoes both the wall and the room detection as a single step"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of all Phase 17 deliverables working together.

Purpose: Confirm that the stepper, properties panel, scene outliner, component catalog, and undo/redo all function correctly before marking the phase complete.
Output: Human verification of the integrated workspace experience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-panel-content-workflow-stepper/17-01-SUMMARY.md
@.planning/phases/17-panel-content-workflow-stepper/17-02-SUMMARY.md
@.planning/phases/17-panel-content-workflow-stepper/17-03-SUMMARY.md
@.planning/phases/17-panel-content-workflow-stepper/17-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and automated checks</name>
  <files>src/components/workspace/workspace-shell.tsx, src/components/workspace/workflow-stepper.tsx, src/components/workspace/properties-panel.tsx, src/components/workspace/scene-outliner.tsx, src/components/workspace/component-catalog.tsx, src/hooks/use-undo-shortcut.ts</files>
  <read_first>
    - src/components/workspace/workspace-shell.tsx
    - src/components/workspace/workflow-stepper.tsx
    - src/components/workspace/properties-panel.tsx
    - src/components/workspace/scene-outliner.tsx
    - src/components/workspace/component-catalog.tsx
    - src/hooks/use-undo-shortcut.ts
  </read_first>
  <action>
    1. Run `pnpm build` and verify zero type errors
    2. Run `pnpm lint` and fix any lint errors in Phase 17 files only:
       - src/components/workspace/workflow-stepper.tsx
       - src/store/selection-store.ts
       - src/components/workspace/properties-panel.tsx
       - src/components/workspace/scene-outliner.tsx
       - src/components/workspace/component-catalog.tsx
       - src/lib/undo/commands/plan-commands.ts
       - src/lib/undo/commands/component-commands.ts
       - src/lib/undo/commands/material-commands.ts
       - src/hooks/use-undo-shortcut.ts
    3. Verify all 9 expected files exist (ls -la each)
    4. Verify workspace-shell.tsx imports all panel components (WorkflowStepper, PropertiesPanel, SceneOutliner, ComponentCatalog, useUndoShortcut)
    5. Verify wall-drawer.tsx imports commandHistory and AddWallCommand (undo wiring)
    6. Verify placed-components.tsx imports commandHistory and PlaceComponentCommand (undo wiring)
    7. Fix any lint or type issues found — limit fixes to Phase 17 files listed above
  </action>
  <verify>
    <automated>cd /c/Users/Nam/BIM && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pnpm build exits with code 0
    - All 9 expected Phase 17 files exist
    - No lint errors in Phase 17 files
  </acceptance_criteria>
  <done>Build passes. All Phase 17 artifacts exist, compile correctly, and pass lint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of integrated Phase 17 workspace</name>
  <files></files>
  <action>
    Present the human with verification steps for all Phase 17 features. Start the dev server if not running.
  </action>
  <verify>
    <automated>cd /c/Users/Nam/BIM && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <what-built>
    Complete Phase 17 workspace integration:
    1. Workflow Stepper — 5-stage breadcrumb at top of workspace
    2. Properties Panel — right dock shows selected element properties
    3. Scene Outliner — left dock tree hierarchy of floors/walls/rooms/components
    4. Component Catalog — left dock categorized preset list with click-to-drag
    5. Undo/Redo — Ctrl+Z/Y keyboard shortcuts wired through wall drawing and component placement
  </what-built>
  <how-to-verify>
    1. Start dev server: `pnpm dev`
    2. Navigate to a building detail page (search for a building first)

    **Stepper verification (FLOW-01):**
    3. Verify a 5-stage horizontal breadcrumb is visible at the very top of the workspace
    4. Click each stage label — it should change the current stage (toolbar items should change)
    5. Verify current stage has primary color highlight, others are dimmed

    **Left dock verification (DISC-01, DISC-02):**
    6. Verify left dock shows "Scene" header with outliner tree below
    7. If walls have been drawn: verify they appear in the tree under the correct floor
    8. Verify component catalog appears below the outliner with category tabs
    9. Click a component in the catalog — verify the cursor changes to placement mode

    **Right dock verification (CTX-01):**
    10. Verify right dock shows "Properties" header
    11. When nothing is selected: verify placeholder "Select an element" message
    12. Click a wall in the 3D scene — verify right dock shows wall properties (thickness, height, thermal conductivity)
    13. Click a placed component — verify right dock shows component properties (preset name, position, rotation)

    **Undo verification (UNDO-01, UNDO-02):**
    14. Draw a wall, press Ctrl+Z — verify the wall disappears
    15. Press Ctrl+Y — verify the wall reappears
    16. Place a component, press Ctrl+Z — verify the component is removed
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms stepper is visible with 5 stages, all clickable
    - Human confirms left dock has outliner and catalog
    - Human confirms right dock has properties panel that updates on 3D click
    - Human confirms Ctrl+Z undoes wall drawing and component placement
    - Human confirms no visual regressions
  </acceptance_criteria>
  <done>Human approves all Phase 17 features are visually present and functional.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- pnpm build passes
- All Phase 17 components render in the workspace
- Stepper shows 5 stages, all clickable
- Left dock has outliner and catalog
- Right dock has properties panel that responds to 3D clicks
- Undo keyboard shortcuts undo wall draws and component placements
</verification>

<success_criteria>
- Human confirms all 6 Phase 17 ROADMAP success criteria are met
- Build passes with zero type errors
- No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/17-panel-content-workflow-stepper/17-05-SUMMARY.md`
</output>
