---
phase: 21-composite-pipeline
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - src/components/viewer/building-scene.tsx
autonomous: false
requirements: [CP-02]

must_haves:
  truths:
    - "A loading indicator is visible in the viewport while footprint or ledger data is in flight"
    - "The loading indicator disappears once the composite 3D scene renders"
    - "If VWorld footprint fetch fails, the building renders as a rectangular box with no error state"
    - "The composite scene renders within 3 seconds of data arrival"
  artifacts:
    - path: "src/components/viewer/building-scene.tsx"
      provides: "BuildingScene accepting footprintData prop, no internal useBuildingFootprint call, composite loading overlay"
      exports: ["BuildingScene", "BuildingSceneProps"]
  key_links:
    - from: "src/app/building/[id]/page.tsx"
      to: "src/components/viewer/building-scene.tsx"
      via: "footprintData prop"
      pattern: "footprintData"
    - from: "src/components/viewer/building-scene.tsx"
      to: "geometry.footprintPolygon"
      via: "footprintData?.polygon passed into useMemo geometry calculation"
      pattern: "footprintData\\.polygon"
---

<objective>
Update BuildingScene to consume the footprintData prop from the page, remove the internal useBuildingFootprint call, add a composite loading overlay, and verify graceful fallback + 3-second render performance.

Purpose: Plan 01 hoisted footprint fetching to page level and threaded the data down as a prop. This plan completes the wiring: BuildingScene reads from the prop instead of fetching internally, adds a visible loading state, and confirms the <3s composite render target (CP-02).

Output: Updated BuildingScene with prop-driven footprint data, loading overlay, fallback behaviour, and a visual checkpoint confirming all four success criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-composite-pipeline/21-01-SUMMARY.md

@src/components/viewer/building-scene.tsx
@src/app/building/[id]/page.tsx
</context>

<interfaces>
<!-- BuildingScene's current prop interface (will be updated by this plan) -->

Current BuildingSceneProps (src/components/viewer/building-scene.tsx, line ~190):
```typescript
interface BuildingSceneProps {
  title: BrTitleInfo;
  floors: BrFloorInfo[];
  campusData?: CampusData;
}
// Plan 01 adds:
//   footprintData?: { polygon: number[][][] | null; error: string | null } | undefined
// Plan 02 makes BuildingScene read from this prop instead of calling useBuildingFootprint internally
```

Current footprint consumption in BuildingScene (lines ~219-220):
```typescript
const { data: footprintData } = useBuildingFootprint(address);
const footprintPolygon = footprintData?.polygon ?? undefined;
```
These two lines must be REPLACED by reading from the prop.

Fallback already works: if footprintPolygon is undefined/null, geo.footprintPolygon stays undefined
and ProceduralBuildingModel renders a rectangular box. Preserve this behaviour.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove internal footprint fetch, consume prop, add loading overlay</name>
  <files>src/components/viewer/building-scene.tsx</files>
  <action>
    Make three targeted changes to `src/components/viewer/building-scene.tsx`:

    **Change 1 — Update BuildingSceneProps interface:**
    Add `footprintData?: { polygon: number[][][] | null; error: string | null } | undefined` to the props interface.
    Add `isCompositeLoading?: boolean` to indicate that either ledger or footprint data is still in flight.
    These are optional props so the component remains backward compatible.

    **Change 2 — Remove internal useBuildingFootprint call:**
    Delete the line: `const { data: footprintData } = useBuildingFootprint(address);`
    Delete the line: `const footprintPolygon = footprintData?.polygon ?? undefined;`
    Delete the import of `useBuildingFootprint` from `@/hooks/use-building-footprint` (no longer needed here).
    Replace with: `const footprintPolygon = footprintData?.polygon ?? undefined;` reading from the prop.

    The `address` variable (line ~218: `const address = title.platPlcNm || title.newPlatPlc || ""`) is no longer needed for the footprint hook — remove it if it has no other consumers in the component. Check the file: if `address` is only used for the footprint hook call, remove it.

    **Change 3 — Add composite loading overlay:**
    In the JSX return, inside the `<div className="relative flex-1 min-h-0">` container (the one that wraps the Canvas), add a loading overlay that appears when `isCompositeLoading` is true:

    ```tsx
    {isCompositeLoading && (
      <div className="absolute inset-0 z-20 flex items-center justify-center bg-background/60 backdrop-blur-sm">
        <div className="flex flex-col items-center gap-3">
          <Loader2 className="size-8 animate-spin text-primary" />
          <p className="text-sm text-muted-foreground">건물 데이터 로딩 중…</p>
        </div>
      </div>
    )}
    ```

    Import `Loader2` from `lucide-react` (add to imports if not already present).

    The overlay sits above the Canvas (`z-20`) so it covers the empty scene during load. It uses `bg-background/60 backdrop-blur-sm` to remain non-jarring. Once `isCompositeLoading` becomes false (both ledger and footprint settled), the overlay disappears and the composite scene is visible.

    **Fallback verification — no code change needed:**
    The existing try/catch in the `geometry` useMemo (lines ~223-257) already handles null footprintPolygon — it leaves `geo.footprintPolygon` undefined, which causes `ProceduralBuildingModel` to render a rectangular box. This fallback is preserved as-is. No additional fallback code is required.

    **Performance note — no code change needed:**
    The <3s render target is architectural: once data arrives, the geometry `useMemo` runs synchronously and R3F renders on the next frame. There are no additional async steps between data arrival and first paint. The SAOPass post-processing runs per-frame and does not delay first render. No performance optimisation is needed beyond ensuring the parallel fetch is wired correctly (done in Plan 01).
  </action>
  <verify>
    <automated>pnpm build 2>&1 | grep -E "error TS|Type error" | head -20</automated>
  </verify>
  <done>
    BuildingScene accepts `footprintData` and `isCompositeLoading` props. Internal `useBuildingFootprint` call is removed. Loading overlay renders when `isCompositeLoading` is true and disappears when false. `pnpm build` passes clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Composite pipeline: parallel ledger + footprint fetches firing from page level, BuildingScene consuming footprintData via prop, loading overlay visible during fetch, graceful rectangular fallback on footprint failure.
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev` and open the app at http://localhost:3000
    2. Open DevTools → Network tab, filter to XHR/Fetch requests
    3. Search for a building and click any result to navigate to its detail page
    4. Observe the Network tab: both a `/api/bldrgst/title` request AND a `/api/vworld/footprint` request should appear. Confirm their "Initiator time" (Waterfall start) is within ~100ms of each other — they should start simultaneously, not sequentially.
    5. Observe the viewport: a loading spinner + "건물 데이터 로딩 중…" text should be visible during the fetch. It should disappear once the 3D building renders.
    6. Confirm the 3D building renders with a real polygon footprint shape (L-shaped, irregular, or rectilinear depending on the selected building) — not just a box.
    7. To test fallback: open DevTools → Network tab → right-click the `/api/vworld/footprint` request → "Block request URL". Reload the page. The building should still render as a rectangular box, with no error banner.
    8. Unblock the URL when done.

    Expected timings:
    - Loading overlay appears immediately on navigation
    - 3D scene renders within 3 seconds of both fetches completing (not counting network round-trip time)
    - No visible flicker or layout shift when overlay disappears
  </how-to-verify>
  <resume-signal>Type "approved" if all four criteria pass, or describe any issues observed</resume-signal>
</task>

</tasks>

<verification>
`pnpm build` — zero TypeScript errors.
`pnpm lint` — zero lint errors.
Visual checkpoint confirms: simultaneous fetch starts, loading indicator, composite render, rectangular fallback.
</verification>

<success_criteria>
1. `BuildingScene` no longer imports or calls `useBuildingFootprint` internally
2. `BuildingScene` reads `footprintData` from props
3. Loading overlay (`Loader2` + "건물 데이터 로딩 중…") is visible when `isCompositeLoading=true`
4. Overlay disappears when both fetches settle and 3D scene is rendered
5. Blocking the VWorld footprint URL in DevTools results in a rectangular building render, not an error
6. Network tab shows ledger + footprint requests starting at the same timestamp
7. `pnpm build` and `pnpm lint` both pass
</success_criteria>

<output>
After completion, create `.planning/phases/21-composite-pipeline/21-02-SUMMARY.md`
</output>
