---
context: default
phase: 03-model-anchors
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-09-15T15:29:13.280Z
quick_id: 260916-0bz
---

# Continue here — paused 2026-09-16 (KST) / 2026-09-15T15:23:03Z

Milestone **v6.0 Building Energy Repository**. Branch
`feat/design-stage-energy-diagnostics` at `8f44281`.

This is **not** a clean stop. A GSD quick task is mid-flight: discussion is
locked, research is complete, there is no PLAN yet, and **no code has
changed**. Do not auto-execute. Do not re-run research.

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Do not auto-execute quick-task 260916-0bz — the user asked to PLAN; executor would `git rm` 52 GLBs (470 MB) and upload to Vercel Blob
- [ ] CONSTRAINT: Do not re-run research — `260916-0bz-RESEARCH.md` exists (HIGH). Next step is the planner. Do not reuse team Blob store `s2mas-blob` (`iad1`)
- [ ] CONSTRAINT: Do not add model twelve until GLBs are out of `public/` — two Vercel ceilings already failed by actual deployment
- [ ] CONSTRAINT: Do not trust a green suite as evidence that an artifact is correct — look at the thing; assert what a string claims

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Auto-execute a "plan" request | User chose Plan GLB blob/CDN migration; gsd-quick would spawn the executor after the planner | blocking | Stop after PLAN.md; wait for explicit confirmation and a Blob token |
| Re-research 260916-0bz | Researcher finished after pause and wrote RESEARCH.md | blocking | Spawn planner from CONTEXT + RESEARCH; skip research |
| Ship model twelve through the app bundle | `public/` is 547.4 MB; 52 GLBs are 470.12 MB; function-size then 8 GB OOM both failed | blocking | Finish 260916-0bz first; Enhanced Builds is a ceiling raise, not a fix |
| Trust green tests over the label | 2026-09-15: floating MEP, 5% HVAC layer, fake certification sentence, next.config include-as-exclude, 110 m² plot stamps as floor area | blocking | Open the page; parse the explanation back to the number |
| Archive v6.0 from `init.milestone-op` | It counts SUMMARY files; Phases 3 and 4 are partial | advisory | Use `init.manager` |
| Re-ask ANCH-03 / SWEEP-01 | User already said they have neither input | advisory | Leave them named-blocked; do not relabel complete |

<current_state>
v6.0 Phases 1, 2 and 5 are production-verified at `0abbdaf` (eleven models,
`region: icn1`, 85 browser cases against https://bim-self.vercel.app).
Phases 3 and 4 stay partial on ANCH-03 and SWEEP-01.

Active work is **quick-task 260916-0bz** (not Phase 03 execution):

- Directory: `.planning/quick/260916-0bz-move-published-reference-building-glbs-o/`
- Flags: `--discuss --research` (not `--validate`, not `--full`)
- CONTEXT.md: written and locked
- RESEARCH.md: written (HIGH) — new public Blob store on `bim` in `icn1`; Next `rewrites()` not a route proxy; `git rm` + gitignore + `.vercelignore`; CLI `blob put`; do not reuse `s2mas-blob`
- PLAN.md: missing
- Source/test changes: none
</current_state>

<completed_work>

Completed this session:
- `/gsd-resume-work` consumed the 2026-09-15 HANDOFF and `.continue-here.md`
- User selected **Plan GLB blob/CDN migration** over waiting on external inputs
- `init.quick` created `260916-0bz` / slug `move-published-reference-building-glbs-o`
- Discussion: **GLBs only** (52 files, 470.12 MB); textures/HDR/wasm stay in `public/`
- CONTEXT.md written with Claude's Discretion (Vercel Blob, same URL path, `git rm` + local fetch script, no history filter, no twelfth model)
- Researcher spawned, then killed by this pause — no RESEARCH.md

Prior session (still true):
- Eleven models in production at `0abbdaf`
- GAP-1 package savings, inferred MEP, atlas normals, West Riverside, Sixty5, function-size exclude
</completed_work>

<remaining_work>

- Spawn `gsd-planner` → `260916-0bz-PLAN.md` honoring CONTEXT.md **and** RESEARCH.md
- **STOP.** Show the plan. Do not spawn the executor until the user confirms.
- ANCH-03 / SWEEP-01 remain blocked-external (do not re-ask)
- Draw-call budget for service layers is still not started
</remaining_work>

<decisions_made>

- GLBs only leave the bundle. Manifests, datasets, flow JSON, SVG, licences stay in git. Textures/HDR/wasm stay in `public/`.
- Hosting: Vercel Blob on `matts-projects-d0677dc4` / https://bim-self.vercel.app (Claude's Discretion). No `@vercel/blob` in the repo today.
- URL contract: keep `/reference-buildings/:id/:file.glb` via rewrite or proxy. Manifest `file` fields stay relative (`model.glb`, `hvac.glb`, …).
- `git rm` the 52 GLBs so the Vercel checkout no longer contains 470 MB of meshes. Local fetch/restore script for Playwright/`next dev`. No git-filter in this task.
- Enhanced Build Machines stays on until an 8 GB builder build is proven. Success is that the 8 GB builder can build, not that the paid setting is immediately turned off.
- Do not add a twelfth model in this task.
- Research: new public Blob store on `bim` in `icn1`; Next `rewrites()` to the public origin (not a route proxy); `git rm` + gitignore + `.vercelignore`; CLI `blob put` with `addRandomSuffix: false`; do not reuse `s2mas-blob` (`iad1`).
</decisions_made>

<blockers>
- ANCH-03: Korean IFC/DXF with redistribution rights — user declined; do not re-ask
- SWEEP-01: actual data.go.kr daily quota — user declined; do not re-ask
- Quick-task executor: blocked on explicit confirmation + `BLOB_READ_WRITE_TOKEN`
- Blob store: `bim` has none; create a public store in `icn1`. Do not reuse `s2mas-blob` (`iad1`). Hostname unknown until create.
</blockers>

## Required Reading (in order)

1. `.planning/quick/260916-0bz-move-published-reference-building-glbs-o/260916-0bz-CONTEXT.md` — locked decisions for the in-flight quick task
1. `.planning/quick/260916-0bz-move-published-reference-building-glbs-o/260916-0bz-RESEARCH.md` — Blob store, rewrite vs proxy, size limits, `.vercelignore`
2. `.planning/HANDOFF.json` — machine-readable resume state
3. `.planning/STATE.md` — milestone position (Phases 1/2/5 verified; 3/4 partial)
4. `docs/04_Agent-Handoffs/CURRENT.md` — production at `0abbdaf` is the authority; ignore the superseded "Sixty5 not deployed" paragraph below the "Supersedes the blocked note below" line
5. `next.config.ts` — `outputFileTracingExcludes` already lists `**/*.glb`; an include only ADDS files
6. `e2e/model-anchors.spec.ts` — asserts `GET /reference-buildings/${id}/model.glb`
7. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses; apply before any assumption analysis

## Critical Anti-Patterns (do NOT repeat these)

- [ANTI-PATTERN]: Treat "plan" as permission to execute a 470 MB `git rm` + Blob upload → Stop after PLAN.md
- [ANTI-PATTERN]: Re-run research because pause said the agent died → RESEARCH.md is on disk; spawn the planner
- [ANTI-PATTERN]: Relabel ANCH-03 / SWEEP-01 complete without the missing inputs → Leave them named-blocked
- [ANTI-PATTERN]: Trust `init.milestone-op` `all_phases_complete: true` → Use `init.manager`

## Infrastructure State

- Production: https://bim-self.vercel.app at `0abbdaf`, eleven models, `icn1`
- Enhanced Build Machines (8 cores / 16 GB): enabled and load-bearing
- No local dev server started this session
- No Vercel Blob token configured in-repo (grep found zero `@vercel/blob`)
- No async-jobs manifests
- Researcher subagent: completed after pause (`01a0a5a7-ffb2-7b81-bfbf-ebd475432ad6`, 102 tool calls). RESEARCH.md is the artifact. Do not wait on it and do not re-run it.

## Pre-Execution Critique Required

- Design artifact: `.planning/quick/260916-0bz-move-published-reference-building-glbs-o/260916-0bz-PLAN.md` (does not exist yet — write it from CONTEXT + RESEARCH)
- Critique focus: leftover on-disk GLBs still OOM because rewrites run after `public/`; do not proxy 70 MB through a route; do not reuse `s2mas-blob` (`iad1`); `BLOB_PUBLIC_BASE_URL` unknown until store create; `.vercelignore` the GLB glob in the same change
- Gate: Do NOT begin execution until the plan exists, the user has confirmed, and `BLOB_READ_WRITE_TOKEN` is available

<context>
Resume restored v6.0 as paused-clean with eleven models in production. The user
did not have the Korean model or the quota figure, and chose the hosting
migration as the work that stops a third Vercel ceiling. Discussion locked the
scope to the 52 GLBs. Everything else (Blob vs R2, URL path, git rm) was left
as Claude's Discretion in CONTEXT.md. Research finished after this pause and
wrote RESEARCH.md. The right resume is: spawn the planner, then wait.
</context>

<next_action>
Start with: spawn the planner for quick-task 260916-0bz from CONTEXT.md +
RESEARCH.md. Do not re-run research. Do not execute.

`/gsd-resume-work`

or, if already in a GSD session:

`/gsd-quick resume move-published-reference-building-glbs-o`
</next_action>
