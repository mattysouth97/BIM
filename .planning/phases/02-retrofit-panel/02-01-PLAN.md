---
phase: 02-retrofit-panel
plan: 01
type: execute
wave: 1
depends_on: ["01-04"]
files_modified:
  - src/components/twin/energy-instrument-hud.tsx
  - src/components/twin/scenario-rail.tsx
  - src/components/twin/retrofit-delta-strip.tsx
  - src/components/twin/measure-chip-row.tsx
  - src/components/twin/selected-measures-strip.tsx
  - src/components/reference-building/reference-retrofit.tsx
  - src/components/twin/retrofit-verification.tsx
  - src/components/twin/capex-input.tsx
  - src/store/scenario-store.ts
autonomous: true
requirements: [PANEL-01, PANEL-02, PANEL-03, PANEL-04, PANEL-05]
must_haves:
  truths:
    - Chosen work has explicit before and after energy and carbon values.
    - Currency savings originate in the shared core's paired engine runs.
    - Source inputs, assumptions and capital verification are visibly distinguished.
    - Corpus position is visibly unavailable and carries no invented benchmark.
    - Unreachable CapexInput and unused default budget constant are absent.
  artifacts:
    - src/components/twin/retrofit-verification.tsx
    - e2e/retrofit-panel.spec.ts
  key_links:
    - useRetrofitScenario.coreResult.delta to paired energy and carbon values
    - coreResult annual bills to annual currency change and basis disclosure
    - appliedMeasureIds to both chosen work labels and core evaluation
---

<objective>
Replace the return-focused retrofit presentation with selected-work outcomes,
evidence and verification guidance, preserving existing bottom-navigation sheets
and desktop drawers. Prepare presentation in parallel; finish only after shared
core integration is verified.
</objective>

<tasks>
<task type="auto" id="tracer">
<name>Chosen-work outcome and real currency tracer</name>
<action>Render before/after site and primary intensity and carbon from the shared
delta, and signed annual bill saving from the paired core bill. Remove NPV, IRR,
payback and aggregate proxy energy from the panel. Preserve optional user-entered
budget as an input, without inventing a default. Keep user work selection and
proposal preview separate.</action>
<verify>Paired-value tests reproduce rendered differences; unsupported core
evaluation withholds currency. LED and PV change outcomes in the browser.</verify>
<done>The user can select work and read its actual modeled outcome.</done>
</task>
<task type="auto" id="evidence">
<name>Evidence, verification and future corpus slot</name>
<action>Expose source origin without promoting all model inputs to measurements;
retain exact lighting/climate/PV disclosures; name assumptions and pre-investment
checks. Show corpus position as not yet available. Use sans typography and full
wrapping in the existing responsive containers. Reference candidate content
reflects chosen work instead of recommendation alone.</action>
<verify>KO/EN tests verify no measured-consumption claim, explicit absence and
verification guidance. Browser checks 390px overflow and scroll reachability.</verify>
<done>Evidence uncertainty remains visible beside the selected-work result.</done>
</task>
<task type="auto" id="cleanup">
<name>Remove obsolete budget artifacts and verify integration</name>
<action>Delete unreachable CapexInput and its implementation-only tests; remove
unused DEFAULT_CAPEX_BUDGET_KRW and make the existing visual test's budget a local
fixture. Run component, shared-core and browser checks; document verified result.</action>
<verify>Source search finds no obsolete component/constant; TypeScript, scoped
lint and relevant unit/browser tests pass.</verify>
<done>PANEL-01 through PANEL-05 are verified without changing drawer mechanics.</done>
</task>
</tasks>

<verification>
Inline plan review: requirements covered; shared core is an explicit integration
dependency; no new finance or physics formula; no production publish; reversible
UI changes need no additional approval under the user's all-phases instruction.
</verification>
