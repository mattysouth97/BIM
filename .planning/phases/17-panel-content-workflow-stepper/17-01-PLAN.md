---
phase: 17-panel-content-workflow-stepper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/workspace/workflow-stepper.tsx
  - src/components/workspace/workspace-shell.tsx
  - package.json
autonomous: true
requirements: [FLOW-01]

must_haves:
  truths:
    - "A horizontal 5-stage breadcrumb is visible at the top of the workspace above the toolbar"
    - "Clicking any stage label navigates to that stage without blocking"
    - "Completed stages show a checkmark icon with green accent"
    - "The current stage is highlighted with primary color; future stages are dimmed"
  artifacts:
    - path: "src/components/workspace/workflow-stepper.tsx"
      provides: "WorkflowStepper component rendering 5-stage breadcrumb"
      min_lines: 60
      exports: ["WorkflowStepper"]
  key_links:
    - from: "src/components/workspace/workflow-stepper.tsx"
      to: "src/store/workflow-store.ts"
      via: "useWorkflowStore selectors for stage and completion"
      pattern: "useWorkflowStore"
    - from: "src/components/workspace/workspace-shell.tsx"
      to: "src/components/workspace/workflow-stepper.tsx"
      via: "renders WorkflowStepper above the toolbar"
      pattern: "WorkflowStepper"
---

<objective>
Create the 5-stage workflow stepper breadcrumb and wire it into the workspace shell above the toolbar strip.

Purpose: Users need a visible pipeline navigator to understand where they are in the authoring flow and jump between stages freely (per D-03 DAG model).
Output: WorkflowStepper component rendered at the top of WorkspaceShell, reading workflow-store for stage/completion state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-panel-content-workflow-stepper/17-CONTEXT.md
@.planning/research/STACK.md

<interfaces>
<!-- From src/store/workflow-store.ts -->
```typescript
interface WorkflowState {
  stage: WorkflowStage;
  completion: Record<WorkflowStage, boolean>;
  setStage: (next: WorkflowStage) => void;
  canAdvance: () => boolean;
  advance: () => void;
  retreat: () => void;
  markComplete: (stage: WorkflowStage) => void;
  resetWorkflow: () => void;
}
```

<!-- From src/lib/workflow/stages.ts -->
```typescript
export type WorkflowStage = "select" | "assemble" | "configure" | "analyze" | "export";
export const STAGE_ORDER: WorkflowStage[];
export const STAGE_LABELS: Record<WorkflowStage, { ko: string; en: string }>;
```

<!-- From src/components/workspace/workspace-shell.tsx -->
```typescript
interface WorkspaceShellProps {
  children: React.ReactNode;
}
export function WorkspaceShell({ children }: WorkspaceShellProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install stepperize and create WorkflowStepper component</name>
  <files>package.json, src/components/workspace/workflow-stepper.tsx</files>
  <read_first>
    - src/store/workflow-store.ts
    - src/lib/workflow/stages.ts
    - .planning/research/STACK.md (stepperize section)
    - src/components/workspace/workspace-shell.tsx
  </read_first>
  <action>
    1. Install stepperize: `pnpm add stepperize`

    2. Create `src/components/workspace/workflow-stepper.tsx`:
       - Import `defineStepper` from stepperize and define the 5 steps using STAGE_ORDER and STAGE_LABELS from `@/lib/workflow/stages`
       - Create a `WorkflowStepper` component that renders a horizontal breadcrumb bar
       - Read `stage` and `completion` from `useWorkflowStore` (per D-04: stepperize headless + custom shadcn-styled breadcrumb)
       - Per D-01: horizontal breadcrumb at the very top of WorkspaceShell, always visible
       - Per D-02: completed stages show CheckCircle2 icon (lucide-react) with `text-green-600` accent; current stage highlighted with `bg-primary text-primary-foreground` badge; future stages use `text-muted-foreground`
       - Per D-03: all stages always clickable — clicking calls `useWorkflowStore.getState().setStage(stage)` directly (DAG model, not linear blocker)
       - Layout: `h-10 flex items-center gap-1 px-4 border-b bg-background` container
       - Each step is a button with: icon (CheckCircle2 if complete, Circle if not), label from STAGE_LABELS (use `.en` for now), chevron separator between steps
       - The stepper must NOT block navigation — all 5 buttons are always enabled regardless of completion state
       - Use `useHydration()` hook before reading store to avoid SSR mismatch (per CLAUDE.md known issues)
  </action>
  <verify>
    <automated>cd /c/Users/Nam/BIM && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "defineStepper\|useStepper" src/components/workspace/workflow-stepper.tsx
    - grep -q "useWorkflowStore" src/components/workspace/workflow-stepper.tsx
    - grep -q "setStage" src/components/workspace/workflow-stepper.tsx
    - grep -q "CheckCircle2\|CheckCircle" src/components/workspace/workflow-stepper.tsx
    - grep -q "text-green" src/components/workspace/workflow-stepper.tsx
    - grep -q "bg-primary" src/components/workspace/workflow-stepper.tsx
    - grep -q "stepperize" package.json
  </acceptance_criteria>
  <done>WorkflowStepper component exists, imports stepperize, reads workflow-store, renders 5 clickable stages with visual state indicators</done>
</task>

<task type="auto">
  <name>Task 2: Wire WorkflowStepper into WorkspaceShell above the toolbar</name>
  <files>src/components/workspace/workspace-shell.tsx</files>
  <read_first>
    - src/components/workspace/workspace-shell.tsx
    - src/components/workspace/workflow-stepper.tsx (just created)
  </read_first>
  <action>
    1. Import `WorkflowStepper` from `./workflow-stepper`
    2. Add `<WorkflowStepper />` as the FIRST child inside the outer `div.flex.h-full.w-full.flex-col` container, ABOVE the `ResizablePanelGroup`
    3. The stepper sits at the very top of the workspace — per D-01: "horizontal breadcrumb bar at the very top of WorkspaceShell (above the toolbar)"
    4. Do NOT modify the ResizablePanelGroup or any dock panel logic — only add the stepper import and JSX element
    5. The stepper div should be a non-resizable strip (fixed height h-10 from the component itself)
  </action>
  <verify>
    <automated>cd /c/Users/Nam/BIM && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "WorkflowStepper" src/components/workspace/workspace-shell.tsx
    - grep -q "workflow-stepper" src/components/workspace/workspace-shell.tsx
  </acceptance_criteria>
  <done>WorkspaceShell renders WorkflowStepper at the top, above the resizable panel group and toolbar. Build passes.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes with no type errors
- WorkflowStepper component exists and exports correctly
- WorkspaceShell imports and renders WorkflowStepper
- stepperize is in package.json dependencies
</verification>

<success_criteria>
- A 5-stage horizontal breadcrumb is rendered at the top of the workspace
- Each stage is clickable and calls setStage()
- Completed stages show green checkmark, current stage has primary color, future stages are dimmed
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-panel-content-workflow-stepper/17-01-SUMMARY.md`
</output>
