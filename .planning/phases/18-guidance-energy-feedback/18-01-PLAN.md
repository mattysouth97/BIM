---
phase: 18-guidance-energy-feedback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/energy/climate-data.ts
  - src/hooks/use-energy-metrics.ts
  - src/components/workspace/status-bar.tsx
  - src/components/workspace/workspace-shell.tsx
autonomous: true
requirements: [ENRG-03, FLOW-03, ENRG-01]
must_haves:
  truths:
    - "Energy calculations use regional HDD/CDD based on building's sido code, not Seoul defaults"
    - "A status bar at the bottom of the workspace shows a contextual prompt that changes based on active tool"
    - "The status bar shows live kWh/m2 with approximate badge that updates when properties change"
  artifacts:
    - path: "src/lib/energy/climate-data.ts"
      provides: "REGIONAL_CLIMATE lookup table keyed by sido code prefix + updated getClimateData(sigunguCd)"
      contains: "REGIONAL_CLIMATE"
    - path: "src/components/workspace/status-bar.tsx"
      provides: "StatusBar component with contextual prompts and energy display"
      exports: ["StatusBar"]
    - path: "src/hooks/use-energy-metrics.ts"
      provides: "Updated hook passing sigunguCd to getClimateData"
      contains: "getClimateData"
  key_links:
    - from: "src/hooks/use-energy-metrics.ts"
      to: "src/lib/energy/climate-data.ts"
      via: "getClimateData(sigunguCd) call"
      pattern: "getClimateData\\("
    - from: "src/components/workspace/status-bar.tsx"
      to: "src/hooks/use-energy-metrics.ts"
      via: "useEnergyMetrics hook consumption"
      pattern: "useEnergyMetrics"
    - from: "src/components/workspace/workspace-shell.tsx"
      to: "src/components/workspace/status-bar.tsx"
      via: "StatusBar rendered in bottom shelf slot"
      pattern: "StatusBar"
---

<objective>
Add regional climate data lookup and build the bottom shelf StatusBar that shows both contextual tool prompts and live energy metrics.

Purpose: Buildings in different Korean regions (Busan, Gangwon, Jeju) will show accurate regional energy calculations instead of Seoul defaults. Users always see what to do next via contextual prompts, and see live energy impact in the persistent status bar.

Output: Updated climate-data.ts with 17-region HDD/CDD table, StatusBar component wired into WorkspaceShell bottom shelf.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-guidance-energy-feedback/18-CONTEXT.md
@.planning/research/ENERGY_FEEDBACK.md
@src/lib/energy/climate-data.ts
@src/hooks/use-energy-metrics.ts
@src/components/workspace/workspace-shell.tsx
@src/store/plan-store.ts
@src/store/authoring-store.ts
@src/lib/workflow/toolbar-configs.ts
@src/lib/korean-building-codes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regional climate data + wire into energy metrics</name>
  <files>src/lib/energy/climate-data.ts, src/hooks/use-energy-metrics.ts</files>
  <read_first>src/lib/energy/climate-data.ts, src/hooks/use-energy-metrics.ts, src/lib/korean-building-codes.ts (for GROUND_TEMPERATURES sido prefix pattern)</read_first>
  <action>
**climate-data.ts changes (per D-07):**

1. Add a `REGIONAL_CLIMATE` lookup table keyed by sido code prefix (2-digit string), containing HDD and CDD for each of the 17 Korean regions. Use KMA-sourced approximate values:
   - "11" (Seoul): { hdd: 2700, cdd: 600 }
   - "26" (Busan): { hdd: 1900, cdd: 750 }
   - "27" (Daegu): { hdd: 2200, cdd: 800 }
   - "28" (Incheon): { hdd: 2750, cdd: 550 }
   - "29" (Gwangju): { hdd: 2150, cdd: 700 }
   - "30" (Daejeon): { hdd: 2400, cdd: 650 }
   - "31" (Ulsan): { hdd: 2050, cdd: 700 }
   - "36" (Sejong): { hdd: 2450, cdd: 650 }
   - "41" (Gyeonggi): { hdd: 2750, cdd: 580 }
   - "43" (Chungbuk): { hdd: 2800, cdd: 620 }
   - "44" (Chungnam): { hdd: 2600, cdd: 630 }
   - "45" (Jeonbuk): { hdd: 2350, cdd: 680 }
   - "46" (Jeonnam): { hdd: 2100, cdd: 720 }
   - "47" (Gyeongbuk): { hdd: 2500, cdd: 680 }
   - "48" (Gyeongnam): { hdd: 2100, cdd: 720 }
   - "50" (Jeju): { hdd: 1600, cdd: 800 }
   - "51" (Gangwon): { hdd: 3400, cdd: 450 }
   - "52" (Jeonbuk new code): { hdd: 2350, cdd: 680 }

   Type the table as `Record<string, { hdd: number; cdd: number }>`.

2. Update `getClimateData()` signature to accept an optional `sigunguCd?: string` parameter. Extract the 2-digit sido prefix via `sigunguCd?.slice(0, 2)`. Look up `REGIONAL_CLIMATE[prefix]`. If found, return a `ClimateData` object using the regional HDD/CDD values but keeping the same winterDesignTemp/summerDesignTemp/indoorTemp/indoorCoolTemp from SEOUL_CLIMATE (these vary less). If not found, fall back to SEOUL_CLIMATE.

3. Export `REGIONAL_CLIMATE` for use by tests and status bar.

**use-energy-metrics.ts changes:**

1. Add a `sigunguCd` parameter to `useEnergyMetrics(buildingPk: string, sigunguCd?: string)`.
2. Pass `sigunguCd` to `getClimateData(sigunguCd)` inside the metrics computation useMemo.
3. Add `sigunguCd` to the useMemo dependency array.
4. No other changes — the hook signature remains backward-compatible (sigunguCd is optional).
  </action>
  <verify>
    <automated>cd C:/Users/Nam/BIM && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "REGIONAL_CLIMATE" src/lib/energy/climate-data.ts (regional lookup table exists)
    - grep -q "sigunguCd" src/lib/energy/climate-data.ts (getClimateData accepts region param)
    - grep -q "sigunguCd" src/hooks/use-energy-metrics.ts (hook passes region through)
    - grep -q '"51"' src/lib/energy/climate-data.ts (Gangwon region with HDD 3400 present)
    - grep -q '"50"' src/lib/energy/climate-data.ts (Jeju region present)
  </acceptance_criteria>
  <done>getClimateData("28xxxxxx") returns Incheon HDD 2750 instead of Seoul 2700. useEnergyMetrics accepts optional sigunguCd. All existing callers still work (param is optional). TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create StatusBar component and wire into WorkspaceShell bottom shelf</name>
  <files>src/components/workspace/status-bar.tsx, src/components/workspace/workspace-shell.tsx</files>
  <read_first>src/components/workspace/workspace-shell.tsx, src/store/plan-store.ts, src/store/authoring-store.ts, src/hooks/use-energy-metrics.ts, src/lib/energy/energy-grade.ts</read_first>
  <action>
**Create src/components/workspace/status-bar.tsx (per D-01, D-02, D-05, D-08):**

A single StatusBar component that fills the 32-40px bottom shelf with two sections:

**Left section — Contextual tool prompt (per D-02, FLOW-03):**
Read from plan-store's `drawingMode` and authoring-store's `annotationMode` to determine the active tool. Map to a one-line prompt string:
- `drawingMode === "wall"` → "Click to place wall start point — Escape to cancel"
- `drawingMode === "opening"` → "Click a wall to place opening — Escape to cancel"
- `annotationMode === "dimension"` → "Click two points to measure — Escape to cancel"
- `annotationMode === "area"` → "Click to mark area boundary — Escape to cancel"
- `annotationMode === "level"` → "Click to set level marker"
- `annotationMode === "section"` → "Drag to set section plane"
- Default (no active tool) → "Ready" or stage-specific hint (e.g., "Select a building to begin" for select stage, "Draw walls to define spaces" for assemble stage)

Display the prompt with a small dot indicator (green when tool active, gray when idle). Use `text-xs text-muted-foreground`.

Korean translations: Use useAppStore language flag. Map each English prompt to Korean equivalent.

**Right section — Energy status bar (per D-05, D-08, ENRG-01):**
- Call `useEnergyMetrics(buildingPk, sigunguCd)` — accept both as props.
- If metrics is null, show a subtle "No building data" placeholder.
- If metrics available, show:
  - Grade badge: colored pill using `gradeColor`, showing grade value (e.g., "2")
  - Demand: `~{demandPerSqm.toFixed(1)} kWh/m2` with tilde prefix per D-08
  - CO2: `~{co2PerSqm.toFixed(1)} kgCO2/m2`
  - Badge: small gray "간이 모델" badge (per D-08) to distinguish from certified ECO2 results
- Use `tabular-nums` for numeric values, `text-xs` sizing.
- Separate left and right sections with flex `justify-between`.

**Props interface:**
```typescript
interface StatusBarProps {
  buildingPk: string;
  sigunguCd?: string;
}
```

**Update workspace-shell.tsx:**
1. Import StatusBar.
2. Replace the bottom shelf placeholder (`<span>Status bar (Phase 18)</span>`) with `<StatusBar buildingPk="" />` (placeholder PK, same pattern as SceneOutliner per Phase 17 decision).
3. Keep the `bottomShelfOpen` conditional rendering and the `h-10 shrink-0 border-t bg-muted/30` container styling, but move the flex/items-center/px-4 into StatusBar itself or keep on the wrapper.
4. The bottom shelf should always be open by default (bottomShelfOpen is already true in workspace-store defaults).
  </action>
  <verify>
    <automated>cd C:/Users/Nam/BIM && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "StatusBar" src/components/workspace/status-bar.tsx (component exists)
    - grep -q "간이 모델" src/components/workspace/status-bar.tsx (approximate model badge per D-08)
    - grep -q "drawingMode" src/components/workspace/status-bar.tsx (reads plan-store for tool prompts)
    - grep -q "useEnergyMetrics" src/components/workspace/status-bar.tsx (consumes energy hook)
    - grep -q "StatusBar" src/components/workspace/workspace-shell.tsx (wired into shell)
    - grep -v "Status bar (Phase 18)" src/components/workspace/workspace-shell.tsx | grep -q "StatusBar" (placeholder replaced)
  </acceptance_criteria>
  <done>Bottom shelf shows contextual tool prompts on the left that change when drawingMode or annotationMode changes. Right side shows live energy grade + kWh/m2 + CO2 with "~" prefix and "간이 모델" badge. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero type errors
2. `pnpm build` — production build passes
3. Verify `getClimateData("51000000")` returns HDD 3400 (Gangwon) not 2700 (Seoul)
4. Verify StatusBar renders in bottom shelf with both prompt and energy sections
</verification>

<success_criteria>
- Regional HDD lookup works for all 17 Korean regions
- Status bar shows contextual prompts that change per active tool
- Energy status bar shows live approximate metrics with disclaimer badge
- All existing energy calculations still work (backward-compatible)
- Build passes with zero type errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-guidance-energy-feedback/18-01-SUMMARY.md`
</output>
