---
phase: 19-coordinate-system-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/vworld/footprint/route.ts
autonomous: true
requirements:
  - GIS-03

must_haves:
  truths:
    - "The VWorld proxy returns HTTP 500 with a clear message when VWORLD_API_KEY is unset"
    - "The domain parameter sent to VWorld is read from VWORLD_DOMAIN env var, falling back to 'localhost'"
    - "No hardcoded domain string 'localhost' remains in any fetch function in footprint/route.ts"
  artifacts:
    - path: "src/app/api/vworld/footprint/route.ts"
      provides: "Parameterized domain and env-var-only API key check"
      contains: "process.env.VWORLD_DOMAIN"
  key_links:
    - from: "src/app/api/vworld/footprint/route.ts"
      to: "process.env.VWORLD_DOMAIN"
      via: "const domain = process.env.VWORLD_DOMAIN ?? 'localhost'"
      pattern: "VWORLD_DOMAIN"
---

<objective>
Harden the existing VWorld footprint proxy route so that:
1. The VWorld `domain` parameter is read from `VWORLD_DOMAIN` environment variable
   (falling back to `"localhost"` for local development) instead of being hardcoded.
2. The existing `VWORLD_API_KEY` 500 guard (already in place at the top of the route)
   is confirmed correct and left intact.

Purpose: Hardcoded `domain: "localhost"` causes silent 401/empty-response failures in
production when the registered VWorld API domain differs from localhost. This is the
security/ops fix that makes all subsequent GIS phases deployable beyond local dev.

Output: Modified `src/app/api/vworld/footprint/route.ts` with domain parameterized.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/PITFALLS.md
@src/app/api/vworld/footprint/route.ts

<interfaces>
<!-- Existing route structure (read from file — do NOT re-read codebase) -->
<!--
  Line 23-29: VWORLD_API_KEY check already implemented — DO NOT CHANGE.
    const apiKey = process.env.VWORLD_API_KEY;
    if (!apiKey) {
      return NextResponse.json(
        { polygon: null, error: "VWORLD_API_KEY environment variable is not set" },
        { status: 500 }
      );
    }

  Lines with hardcoded domain (THREE occurrences — all must be replaced):
    fetchByPNU:           url.searchParams.set("domain", "localhost");   // line ~121
    fetchByBBox:          url.searchParams.set("domain", "localhost");   // line ~144
    fetchByExplicitBBox:  url.searchParams.set("domain", "localhost");   // line ~172
-->

<!-- Pattern from PITFALLS.md (Pitfall 8):
  const domain = process.env.VWORLD_DOMAIN ?? "localhost";
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize domain in footprint/route.ts</name>
  <files>src/app/api/vworld/footprint/route.ts</files>
  <action>
    Make exactly two types of changes to `src/app/api/vworld/footprint/route.ts`.
    Do NOT change anything else — routing logic, response shapes, error handling, and
    extractPolygon() are all out of scope for this plan.

    **Change 1 — Add domain constant near the top of the file** (after the VWORLD_DATA_URL
    const declaration, before the GET function):

    ```typescript
    const VWORLD_DOMAIN = process.env.VWORLD_DOMAIN ?? "localhost";
    ```

    **Change 2 — Replace all three hardcoded domain lines** in the three private fetch
    functions. Each occurrence looks like:
      url.searchParams.set("domain", "localhost");
    Replace ALL THREE with:
      url.searchParams.set("domain", VWORLD_DOMAIN);

    The three occurrences are in:
    - fetchByPNU()
    - fetchByBBox()
    - fetchByExplicitBBox()

    Verify the VWORLD_API_KEY guard at lines 23-29 is still intact after edits —
    it must remain unchanged:
      const apiKey = process.env.VWORLD_API_KEY;
      if (!apiKey) { return NextResponse.json(..., { status: 500 }); }

    After edits, grep the file to confirm zero remaining occurrences of
    the string `"localhost"` as a hardcoded value anywhere in the file.
  </action>
  <verify>
    <automated>grep -c '"localhost"' src/app/api/vworld/footprint/route.ts || true</automated>
    Expected output: 0 (no hardcoded "localhost" strings remaining)

    Also run: pnpm build
    Expected: exits 0 with no TypeScript errors.
  </verify>
  <done>
    - `src/app/api/vworld/footprint/route.ts` contains `process.env.VWORLD_DOMAIN` in exactly one place (the const declaration)
    - `grep "localhost" src/app/api/vworld/footprint/route.ts` returns no matches
    - `grep "VWORLD_API_KEY" src/app/api/vworld/footprint/route.ts` still returns the existing guard
    - `pnpm build` exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify 500 guard and domain behavior via build + grep assertions</name>
  <files>src/app/api/vworld/footprint/route.ts</files>
  <behavior>
    - When VWORLD_API_KEY is unset, GET handler returns status 500 with
      { polygon: null, error: "VWORLD_API_KEY environment variable is not set" }
    - The domain sent to VWorld is controlled by VWORLD_DOMAIN env var
    - No hardcoded credential strings in source
  </behavior>
  <action>
    Run the following assertions to confirm correctness after Task 1:

    1. Confirm the 500 guard exists and has the correct error message:
       grep -n "VWORLD_API_KEY environment variable is not set" src/app/api/vworld/footprint/route.ts
       Expected: prints at least one matching line.

    2. Confirm domain is parameterized (not hardcoded):
       grep -n "VWORLD_DOMAIN" src/app/api/vworld/footprint/route.ts
       Expected: prints at least one matching line.

    3. Confirm no hardcoded "localhost" remains:
       grep -c '"localhost"' src/app/api/vworld/footprint/route.ts
       Expected: 0

    4. Confirm no hardcoded API key string (the old key starts with "98E6A75B"):
       grep -c "98E6A75B" src/app/api/vworld/footprint/route.ts
       Expected: 0

    5. Final build check:
       pnpm build
       Expected: exits 0.

    If any assertion fails, return to Task 1 and fix the relevant issue before proceeding.
  </action>
  <verify>
    <automated>grep -c '"localhost"' src/app/api/vworld/footprint/route.ts && grep -c "98E6A75B" src/app/api/vworld/footprint/route.ts || pnpm build</automated>
    All grep counts return 0; pnpm build exits 0.
  </verify>
  <done>
    All five grep assertions pass (correct output for each).
    `pnpm build` exits 0 with no TypeScript errors introduced.
    The route is safe to deploy: no hardcoded credentials or domain strings.
  </done>
</task>

</tasks>

<verification>
Run in order:
1. `grep -c '"localhost"' src/app/api/vworld/footprint/route.ts` → must return 0
2. `grep -c "98E6A75B" src/app/api/vworld/footprint/route.ts` → must return 0
3. `grep "VWORLD_DOMAIN" src/app/api/vworld/footprint/route.ts` → must show the const declaration
4. `grep "VWORLD_API_KEY environment variable is not set" src/app/api/vworld/footprint/route.ts` → must show the error message
5. `pnpm build` → must exit 0
</verification>

<success_criteria>
- `src/app/api/vworld/footprint/route.ts` reads domain from `process.env.VWORLD_DOMAIN ?? "localhost"`
- `VWORLD_API_KEY` missing → route returns 500 with descriptive error (existing behavior preserved)
- Zero hardcoded credential strings in the file
- `pnpm build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/19-coordinate-system-foundation/19-02-SUMMARY.md`
</output>
