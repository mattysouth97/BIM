---
phase: 06-interactive-configuration-panel
plan: 02
type: execute
wave: 1
depends_on: [1]
files_modified:
  - src/components/viewer/config-panel.tsx
  - src/components/viewer/config-tabs/building-tab.tsx
  - src/components/viewer/config-tabs/envelope-tab.tsx
autonomous: true
---

# Plan 02: Building Tab + Envelope Tab

<objective>
Implement the Building tab (recipe geometry controls) and Envelope tab (material property controls) with live sliders, dropdowns, and reset buttons. Changes immediately update stores.
</objective>

<tasks>

<task id="1">
<title>Building tab with recipe controls</title>
<read_first>
- src/components/viewer/config-panel.tsx
- src/store/recipe-store.ts
- src/lib/procedural/types.ts
- src/lib/procedural/recipe.ts
- src/components/ui/slider.tsx
- src/components/ui/select.tsx
</read_first>
<action>
Create `src/components/viewer/config-tabs/building-tab.tsx`:
- Props: `{ buildingPk: string }`
- Uses useRecipeStore to read/write overrides
- Sections with SliderRow components:

**Geometry section:**
- Footprint Width: slider 4-50m, step 0.5
- Footprint Depth: slider 4-50m, step 0.5
- Floor Count: slider 1-30, step 1 (integer)
- Floor Height: slider 2.5-5.0m, step 0.1

**Facade section:**
- Window Ratio: slider 10-80%, step 5
- Mullion Depth: slider 0.02-0.15m, step 0.01
- Sill Height: slider 0.5-1.2m, step 0.05
- Solid Panel Chance: slider 0-50%, step 5
- Parapet Height: slider 0-1.5m, step 0.1

**Structure section:**
- Column Spacing: slider 3-10m, step 0.5
- Column Size: slider 0.2-0.8m, step 0.05
- Slab Thickness: slider 0.1-0.4m, step 0.02
- Wall Thickness: slider 0.1-0.5m, step 0.02

**Roof section:**
- Roof Type: dropdown (flat/gable)
- Gable Height: slider 0-5m, step 0.5 (visible only when gable selected)

Each slider: label on left, current value on right, shadcn Slider below
"Reset to Defaults" button at bottom — calls resetOverrides(pk)
Bilingual labels (isKo)

Create a shared `SliderRow` component:
- Props: label: string, value: number, min: number, max: number, step: number, unit: string, onChange: (v: number) => void
- Renders: label + value display + Slider
</action>
<acceptance_criteria>
- `src/components/viewer/config-tabs/building-tab.tsx` contains `export function BuildingTab`
- Has Geometry, Facade, Structure, Roof sections
- Uses shadcn Slider component
- Roof type uses dropdown (Select component)
- Gable height conditionally visible
- Reset button calls resetOverrides
- `pnpm build` passes
</acceptance_criteria>
</task>

<task id="2">
<title>Envelope tab with material controls</title>
<read_first>
- src/components/viewer/material-panel.tsx
- src/store/material-store.ts
- src/lib/korean-building-codes.ts
- src/lib/material-inference.ts
- src/lib/material-types.ts
</read_first>
<action>
Create `src/components/viewer/config-tabs/envelope-tab.tsx`:
- Props: `{ buildingPk: string }`
- Uses useMaterialStore to read/write properties via overrideProperty

**Wall section:**
- Wall U-value: slider 0.12-2.5 W/(m2K), step 0.01
- Insulation Type: dropdown from WALL_LAYERS presets (EPS, XPS, PIR, Glass Wool — show Korean names)
  - Selecting a preset auto-updates U-value based on WALL_LAYERS data

**Window section:**
- Window U-value: slider 0.8-5.0 W/(m2K), step 0.1
- SHGC: slider 0.1-0.9, step 0.05
- WWR: slider 10-80%, step 5
- Glass Type: dropdown (single/double/triple/low-e)

**Roof & Floor section:**
- Roof U-value: slider 0.1-2.0, step 0.01
- Floor U-value: slider 0.15-2.0, step 0.01

**Airtightness section:**
- ACH50: slider 0.5-10.0, step 0.5

"Reset to Code Defaults" button — calls setProperties(pk, inferMaterialProperties(title, floors))
Source badge shows "User Input" when any value edited

Reuse SliderRow from building-tab (move to shared location or keep in same dir)
</action>
<acceptance_criteria>
- `src/components/viewer/config-tabs/envelope-tab.tsx` contains `export function EnvelopeTab`
- Has Wall, Window, Roof/Floor, Airtightness sections
- Insulation dropdown shows Korean names from WALL_LAYERS
- Glass type dropdown with 4 options
- Reset button restores inferMaterialProperties defaults
- `pnpm build` passes
</acceptance_criteria>
</task>

<task id="3">
<title>Wire tabs into ConfigPanel</title>
<read_first>
- src/components/viewer/config-panel.tsx
- src/components/viewer/config-tabs/building-tab.tsx
- src/components/viewer/config-tabs/envelope-tab.tsx
</read_first>
<action>
Update `src/components/viewer/config-panel.tsx`:
- Import BuildingTab and EnvelopeTab
- Replace Building tab placeholder with `<BuildingTab buildingPk={buildingPk} />`
- Replace Envelope tab placeholder with `<EnvelopeTab buildingPk={buildingPk} />`
- Keep Systems and Layers tabs as placeholders for Plan 03
</action>
<acceptance_criteria>
- config-panel.tsx imports and renders BuildingTab and EnvelopeTab
- Building tab shows recipe sliders
- Envelope tab shows material sliders
- `pnpm build` passes
</acceptance_criteria>
</task>

</tasks>

<verification>
- Building tab renders all recipe parameter sliders with correct ranges
- Envelope tab renders all material property sliders
- Slider changes update respective stores
- Reset buttons work
- `pnpm build` passes
</verification>

<must_haves>
- Building tab with geometry, facade, structure, roof sections
- Envelope tab with wall, window, roof/floor, airtightness sections
- Slider ranges matching Korean building code limits
- Insulation preset dropdown with Korean names
- Reset to defaults functionality
</must_haves>
