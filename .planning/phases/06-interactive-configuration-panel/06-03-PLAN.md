---
phase: 06-interactive-configuration-panel
plan: 03
type: execute
wave: 2
depends_on: [1, 2]
files_modified:
  - src/components/viewer/config-tabs/systems-tab.tsx
  - src/components/viewer/config-tabs/layers-tab.tsx
  - src/components/viewer/config-panel.tsx
  - src/components/viewer/building-scene.tsx
  - src/components/viewer/procedural-building-model.tsx
  - src/components/viewer/building-layers.tsx
autonomous: true
---

# Plan 03: Systems Tab + Layers Tab + 3D Integration

<objective>
Implement Systems tab (HVAC/lighting/renewables), Layers tab (per-layer density controls), and wire recipe overrides to trigger ProceduralBuilding regeneration and layer regeneration in the 3D scene.
</objective>

<tasks>

<task id="1">
<title>Systems tab with HVAC/lighting/renewables</title>
<read_first>
- src/store/material-store.ts
- src/lib/korean-building-codes.ts
- src/lib/material-types.ts
- src/components/viewer/config-tabs/building-tab.tsx
</read_first>
<action>
Create `src/components/viewer/config-tabs/systems-tab.tsx`:
- Props: `{ buildingPk: string }`
- Uses useMaterialStore for HVAC/lighting properties

**HVAC section:**
- System Type: dropdown (central/individual/VRF/district — from HVAC_DEFAULTS keys)
- Heating Efficiency: slider 60-98%, step 1
- Cooling COP: slider 2.0-6.0, step 0.1
- Korean names for system types

**Lighting section:**
- Lighting Power Density: slider 5-25 W/m2, step 0.5
- Control Type: dropdown (manual/occupancy-sensor/daylight-dimming)

**Occupancy section:**
- Occupancy Density: slider 5-50 m2/person, step 1

**Renewables section:**
- Solar Panel Area: slider 0-500 m2, step 10
- System Type: dropdown (none/rooftop-PV/BIPV)

"Reset to Defaults" button
Bilingual labels
</action>
<acceptance_criteria>
- `src/components/viewer/config-tabs/systems-tab.tsx` contains `export function SystemsTab`
- Has HVAC, Lighting, Occupancy, Renewables sections
- HVAC system type dropdown with Korean names
- Solar panel area slider
- `pnpm build` passes
</acceptance_criteria>
</task>

<task id="2">
<title>Layers tab with density controls</title>
<read_first>
- src/lib/layers/types.ts
- src/store/layer-store.ts
- src/lib/layers/layer-manager.ts
- src/components/viewer/config-tabs/building-tab.tsx
</read_first>
<action>
Create `src/components/viewer/config-tabs/layers-tab.tsx`:
- Props: `{ buildingPk: string }`
- Uses useLayerStore for visibility
- For each of the 10 layers from LAYER_CONFIGS:
  - Row: colored dot (layer color), layer name, visibility toggle, density slider
  - Density slider: 0-100% range, step 10, default 50%
  - Density label varies by layer type:
    - L1: "Detail Level", L2: "Pipe Density", L3: "Sensor Spacing"
    - L4: "Elevator Count", L5: "Sprinkler Density", L6: "Conduit Count"
    - L7: "Battery Units", L8: "Node Density", L9: "Chute Count", L10: "Panel Detail"
  - Slider onChange: store density in a new layerDensity state in layer-store, trigger layer regeneration

Update `src/store/layer-store.ts`:
- Add `density: Record<LayerId, number>` state (default all 50)
- Add `setDensity(id: LayerId, value: number)` action
- Density used by layer generators to scale element counts
</action>
<acceptance_criteria>
- `src/components/viewer/config-tabs/layers-tab.tsx` contains `export function LayersTab`
- Renders 10 layer rows with visibility toggle + density slider
- Each layer has a context-appropriate density label
- layer-store.ts has density state and setDensity action
- `pnpm build` passes
</acceptance_criteria>
</task>

<task id="3">
<title>Wire ConfigPanel tabs + recipe overrides to 3D scene</title>
<read_first>
- src/components/viewer/config-panel.tsx
- src/components/viewer/building-scene.tsx
- src/components/viewer/procedural-building-model.tsx
- src/components/viewer/building-layers.tsx
- src/lib/procedural/recipe.ts
- src/store/recipe-store.ts
</read_first>
<action>
Update `src/components/viewer/config-panel.tsx`:
- Import SystemsTab and LayersTab
- Replace Systems tab placeholder with `<SystemsTab buildingPk={buildingPk} />`
- Replace Layers tab placeholder with `<LayersTab buildingPk={buildingPk} />`

Update `src/components/viewer/building-scene.tsx`:
- Import useRecipeStore
- Read overrides for current buildingPk: `const recipeOverrides = useRecipeStore(s => s.overrides[buildingPk])`
- Apply overrides to recipe: `const recipe = useMemo(() => { const base = toRecipe(geometry); return recipeOverrides ? applyOverrides(base, recipeOverrides) : base; }, [geometry, recipeOverrides])`
- Import applyOverrides from @/lib/procedural/recipe
- Pass updated recipe to both ProceduralBuildingModel and BuildingLayers

Update `src/components/viewer/procedural-building-model.tsx`:
- Accept optional `recipeOverride?: BuildingRecipe` prop
- If recipeOverride provided, use it instead of computing from geometry
- OR: accept recipe prop directly instead of geometry (cleaner)

Update `src/components/viewer/building-layers.tsx`:
- Subscribe to layer-store density changes
- When density changes for a visible layer: call layerManager.disposeLayer(id) then getOrGenerate(id, recipe) to regenerate with new density
- Pass density to layer generators (extend LayerGenerator.generate to accept optional density param)
</action>
<acceptance_criteria>
- config-panel.tsx renders all 4 tabs with real content
- building-scene.tsx applies recipe overrides from useRecipeStore
- Changing a recipe slider triggers ProceduralBuilding regeneration
- Changing layer density triggers layer regeneration
- `pnpm build` passes
</acceptance_criteria>
</task>

</tasks>

<verification>
- All 4 config tabs render with real controls
- Recipe changes (footprint, floors, facade) update 3D model live
- Material property changes update material store
- Layer density changes regenerate layer geometry
- Reset buttons restore defaults
- `pnpm build` passes
</verification>

<must_haves>
- Systems tab with HVAC, lighting, occupancy, renewables
- Layers tab with per-layer density sliders
- Recipe overrides flow from store → applyOverrides → ProceduralBuilding
- Layer density changes trigger regeneration
- All 4 tabs wired into ConfigPanel
</must_haves>
