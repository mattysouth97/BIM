---
phase: 03-model-anchors
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [ANCH-01, ANCH-02, ANCH-07]
files_modified:
  - src/lib/reference-buildings/registry.json
  - src/lib/reference-buildings/manifest.ts
  - src/lib/landing/gallery.ts
  - scripts/build-gallery-catalogue.mjs
  - public/reference-buildings/*/manifest.json
must_haves:
  truths:
    - Every displayed card quantity and datum comes from its generated manifest.
    - A contract checks every registered building and fails on stale generated data.
    - All cards explicitly state that no measured energy series is linked.
---

<objective>Replace hand-synchronised gallery and model rosters with one registry and a compact generated gallery projection.</objective>
<tasks>
<task type="auto"><name>Manifest-to-gallery tracer</name><action>Preserve the GalleryItem interface. Generate compact data from manifests, reconcile floor-space rows from spaces.json, and derive numeric labels. Keep metadata editorial, never copy numeric figures into it. Registry keys own routing and enumeration.</action><verify>All-model manifest contract; deliberately changed manifest fixture changes the projected figure.</verify><done>Gallery, route allowlist and dataset catalogue enumerate one registry.</done></task>
<task type="auto"><name>Honest evidence disclosure</name><action>Add an explicit no-linked-measured-energy state and coordinate its rendering with the parallel gallery presentation worker. Preserve full licence attribution.</action><verify>KO/EN component and mobile browser assertions after integration.</verify><done>No card implies calibration from geometry measurements.</done></task>
</tasks>

Boundary: no physics edits, deployment, global state edits, or invention of missing source classifications. Existing generated IFC space rows can enrich manifest summaries without retessellating unchanged geometry; record generator and source hashes.
