---
phase: 05-publishing
plan: 01
wave: 1
type: execute
requirements: [PUB-01, PUB-02, PUB-03, PUB-04, PUB-05, PUB-06, PUB-07, PUB-09]
---
# Reviewed durable corpus publication

Consume Phase 4 schema and genuine generated records. Implement a strict public field allowlist, nonempty snapshot/coverage/lineage validation, hash-bound review, immutable atomic Neon storage, parameterized read-only filtered/paginated APIs, whole-release export and a field/unit dictionary. Database errors expose no credentials or SQL. No public mutation route. Keep artifacts outside git; reject empty or test-only publication. Separate internal consistency from unvalidated stock accuracy. Validate unit/route contracts and a real database connection. Inspect the concrete first release before publication; no generated substitute if upstream data is absent.

Neon connection on 2026-09-15: parameterized HTTPS protocol SELECT 1, HTTP 200, expected row. API protocol reference: https://github.com/neondatabase/serverless/blob/main/src/httpQuery.ts . No new dependency; existing runtime fetch and JSON values suffice.
