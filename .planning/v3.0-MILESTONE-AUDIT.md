---
milestone: v3.0
audited: 2026-03-31
status: tech_debt
scores:
  requirements: 14/17
  phases: 5/5
  integration: 22/25
  flows: 4/7
gaps:
  requirements:
    - id: "ENRG-01"
      status: "partial"
      phase: "Phase 18"
      verification_status: "passed"
      evidence: "StatusBar component works but buildingPk is hardcoded as empty string — energy section always shows 'No building data'"
    - id: "ENRG-03"
      status: "partial"
      phase: "Phase 18"
      verification_status: "passed"
      evidence: "REGIONAL_CLIMATE table and getClimateData() work correctly, but sigunguCd never flows from page.tsx to StatusBar — always uses Seoul defaults"
    - id: "FLOW-01"
      status: "partial"
      phase: "Phase 17"
      verification_status: "human_needed"
      evidence: "Stepper navigates correctly but markComplete() is never called — stages never show green checkmarks"
  integration:
    - from: "page.tsx"
      to: "WorkspaceShell"
      issue: "buildingPk and sigunguCd not propagated as props — 3 downstream consumers receive empty values"
    - from: "wall-drawer.tsx"
      to: "workflow-store.markComplete"
      issue: "Stage completion never fires — stepper checkmarks never appear"
    - from: "building-scene.tsx"
      to: "workspace properties-panel"
      issue: "Old viewer/properties-panel.tsx still renders alongside new workspace/properties-panel.tsx — UI duplication"
tech_debt:
  - phase: 14-workflow-state-foundation
    items:
      - "canAdvance(), advance(), retreat() are unreachable dead code — DAG model uses setStage() directly"
      - "STAGE_GUARDS always return true — guard system structurally inert"
  - phase: 16-contextual-toolbar-migration
    items:
      - "Old viewer/properties-panel.tsx still exists and renders when isAuthoring — duplicate UI path"
  - phase: 17-panel-content-workflow-stepper
    items:
      - "markComplete() never called — stepper completion state is permanently gray"
      - "DISC-02 uses click-to-place, not HTML5 drag — intent satisfied but label mismatch"
  - phase: 18-guidance-energy-feedback
    items:
      - "buildingPk hardcoded as empty string in WorkspaceShell — energy status bar always null"
      - "sigunguCd not threaded from page.tsx — regional climate always Seoul defaults"
---

# Milestone v3.0: UX Workflow Overhaul — Audit Report

## Summary

**Status:** Tech Debt (no critical blockers, 3 partial requirements, accumulated debt)

All 5 phases completed. 14 of 17 requirements fully satisfied. 3 requirements are partially satisfied — the underlying implementations are correct but prop threading from the page route to workspace components is incomplete, causing runtime gaps in energy feedback and stepper completion.

## Requirements Coverage

| Requirement | Phase | Status | Evidence |
|-------------|-------|--------|----------|
| LAYOUT-01 | 15 | ✓ Satisfied | Viewport-dominant layout with docked panels |
| LAYOUT-02 | 15 | ✓ Satisfied | Resizable left/right docks via react-resizable-panels |
| LAYOUT-03 | 15 | ✓ Satisfied | Collapsible docks with hidden prop pattern |
| FLOW-01 | 17 | ⚡ Partial | Stepper navigates but completion checkmarks never appear |
| FLOW-02 | 16 | ✓ Satisfied | Mode indicator badge in contextual toolbar |
| FLOW-03 | 18 | ✓ Satisfied | Status bar with contextual tool prompts |
| CTX-01 | 17 | ✓ Satisfied | Click wall/component → properties in right dock |
| CTX-02 | 16 | ✓ Satisfied | TOOLBAR_CONFIGS[stage] drives toolbar rendering |
| CTX-03 | 16 | ✓ Satisfied | viewer-overlay.tsx deleted, toolbar-configs.ts replaces it |
| DISC-01 | 17 | ✓ Satisfied | Scene outliner with floor/room/wall tree |
| DISC-02 | 17 | ✓ Satisfied | Component catalog with click-to-place |
| DISC-03 | 18 | ✓ Satisfied | driver.js 4-step onboarding tour |
| UNDO-01 | 17 | ✓ Satisfied | Ctrl+Z/Y across wall, component, material |
| UNDO-02 | 17 | ✓ Satisfied | Compound undo (wall + room detection) |
| ENRG-01 | 18 | ⚡ Partial | StatusBar exists but buildingPk="" → always null |
| ENRG-02 | 18 | ✓ Satisfied | useEnergyDelta with inline delta annotations |
| ENRG-03 | 18 | ⚡ Partial | REGIONAL_CLIMATE table exists but sigunguCd never threaded |

## Phase Verification Summary

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 14 | human_needed | 4/4 auto | E2E tests pending |
| 15 | passed | 9/9 | All checks passed |
| 16 | passed (after gap closure) | 11/11 | TOOLBAR_CONFIGS wired via 16-03 |
| 17 | human_needed | 6/6 auto | Visual verification pending |
| 18 | passed | 8/8 | All checks passed |

## Cross-Phase Integration Gaps

| From | To | Issue | Fix Scope |
|------|----|-------|-----------|
| page.tsx | WorkspaceShell | buildingPk + sigunguCd not propagated as props | 2 files, ~10 lines |
| wall-drawer.tsx | workflow-store.markComplete | Stage completion never fires | 1 file, ~3 lines |
| building-scene.tsx | workspace properties-panel | Old viewer panel still renders alongside new | Remove old import/render |

## Tech Debt by Phase

### Phase 14: Workflow State Foundation
- `canAdvance()`, `advance()`, `retreat()` are dead code (DAG model uses `setStage()`)
- `STAGE_GUARDS` always return true — guard system is structurally inert

### Phase 16: Contextual Toolbar Migration
- Old `viewer/properties-panel.tsx` still exists and renders when `isAuthoring` — duplicate UI

### Phase 17: Panel Content + Workflow Stepper
- `markComplete()` never called — stepper never shows completion state
- DISC-02 uses click-to-place, not HTML5 drag (intent satisfied)

### Phase 18: Guidance + Energy Feedback
- `buildingPk` hardcoded as `""` in WorkspaceShell — energy status bar always null
- `sigunguCd` not threaded from page route — regional climate always Seoul

## Recommended Fix (3 files, ~20 lines)

1. `workspace-shell.tsx` — Add `buildingPk` and `sigunguCd` props, thread to StatusBar + SceneOutliner
2. `page.tsx` — Pass `buildingPk={mgmBldrgstPk}` and `sigunguCd={buildingId.sigunguCd}` to WorkspaceShell
3. `wall-drawer.tsx` — Call `markComplete("assemble")` after compound wall commit

---
*Audited: 2026-03-31*
