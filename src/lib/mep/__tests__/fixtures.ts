// src/lib/mep/__tests__/fixtures.ts
// The six-case QA building set now lives in src/lib/mep/qa-cases.ts so the
// /dev/mep visual-QA harness renders exactly what the tests assert on.
export * from "../qa-cases";
