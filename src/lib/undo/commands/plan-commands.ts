// plan-commands.ts
// NOTE: The plan/wall-drawing system (plan-store, WallSegment, Opening, Room)
// was removed in v5.0 cleanup. This file is retained as an empty module to
// avoid breaking the undo command namespace structure. Commands will be
// re-implemented in Phase 31 (Annotation Lifecycle) once the store is restored.

export {};
