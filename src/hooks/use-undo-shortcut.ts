"use client";

/**
 * Registers Ctrl+Z / Ctrl+Y keyboard shortcuts.
 * Undo/redo system removed in Phase 19 pivot to Digital Twin platform.
 * Hook retained as a no-op to avoid breaking WorkspaceShell call site.
 */
export function useUndoShortcut(): void {
  // No-op: undo/redo system removed
}
